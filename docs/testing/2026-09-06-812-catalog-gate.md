# #812 — Protección de campos técnicos del catálogo

> **[Evidencia de sesión · 2026-09-06 · aplicada y verificada en producción]**

La lectura de `pg_get_functiondef` confirma que dev ya tiene el contenido de
`20260878_catalog_technical_columns_gate.sql` y producción conserva la versión anterior.
No se deduce este estado del ledger de migraciones.

| Entorno antes del cambio | md5(prosrc) |
| --- | --- |
| Dev `tyvzpuhxfwxrnkcpzxyg` | `9c43b63cf06a8b8a1abe4c579e9a6996` |
| Producción `vmutcradmodhiltuohys` | `0e822f261c34a28d9e5fc7afafee198f` |

La migración existente conserva las reglas de ficha y protege las transiciones de
campos técnicos ya poblados. Permite rellenar valores inicialmente nulos y repetir
el mismo valor. No modifica filas, grants ni políticas.

## Verificación

`supabase/tests/catalog_technical_gate.sql` usa filas desechables con rollback,
identidad de usuario ordinario y el trigger real. Se ejecuta en la base local,
nunca contra datos de producción.

- RED: al instalar dentro de una transacción la función histórica, falla con
  `FAIL: populated book hydration must be protected`. La transacción se revierte.
- GREEN: con la migración prevista pasan la hidratación inicial, la escritura sin
  cambios y las cinco transiciones protegidas de libros, películas y series.
- `node scripts/db/verify.mjs`: PASS, 236 pasos de bootstrap, contratos y permisos;
  incorpora esta regresión a la CI existente del esquema.

La revisión automática bloqueó la primera aplicación remota por exigir autorización
explícita para producción. Tras recibirla, la misma operación `apply_migration`
devolvió `success: true`. No se usó otra vía.

Después de aplicar: `md5(prosrc) = 72d2695392d26f2b4f4eb13a2073c049` en producción
(el fichero enviado contiene CRLF). Comparando el cuerpo con saltos LF normalizados,
dev y producción coinciden: `9c43b63cf06a8b8a1abe4c579e9a6996`, verificado por
consulta posterior en ambos proyectos.
Los triggers `trg_enforce_books_edit_collaborator_only`,
`trg_enforce_movies_edit_collaborator_only` y `trg_enforce_series_edit_collaborator_only`
están habilitados (`O`), BEFORE UPDATE por fila, y llaman a la función corregida.
No se modificaron datos de catálogo ni se ejecutaron fixtures en producción.
