# Registro de decisiones — Biblioshare

> **[Histórico · append-only]** No se reescribe: una decisión superada se deroga con una entrada nueva, nunca editando la vieja. Por eso no puede desincronizarse.

> **[Historia · append-only]** No se reescribe: solo se añaden entradas nuevas al final. Explica *por qué* se decidió cada cosa. Para el estado *actual* del esquema manda `data-model.md`; para el de las features, `backlog.md`.

## Decisiones de arquitectura (de forma)

Decisiones de forma que, tomadas tarde (con datos o UI ya construidos encima), costarían un refactor. No son features. La mayoría se resolvió (8-A, 8-B, 8-C, 8-F, 8-G, 8-H) o confirmó su enfoque (8-D, 8-E); queda abierta la normalización de géneros (dentro de 8-B).

### 8-A. ¿"Pausado" es un estado explícito o inferido? — *decidido*
`paused` es un **estado explícito** en el enum `media_status` (migración barata vía `ALTER TYPE ... ADD VALUE`). Evita heurísticas de inactividad frágiles y no obliga a re-modelar el filtro de estados (`LibraryFilters`, `ProgressPanel`, `PublicItemCard`) dos veces.
**Añadido**: cuando exista 7.14 (`progress_sessions`) esa señal puede **sugerir** el cambio (p. ej. "sin avance en 45 días, ¿lo marcamos como pausado?" dentro de 7.17), nunca aplicarlo sola: el usuario decide el estado, el sistema solo detecta y avisa.

### 8-B. Relaciones entre ítems de catálogo (adaptaciones, mismo universo, recomendaciones) — *decidido*
Si se aborda 7.21 o 7.19, usar una **tabla genérica de relaciones** (`item_type_a/item_id_a`, `item_type_b/item_id_b`, `relation_type`) en vez de columnas ad-hoc por tipo — mismo espíritu que unificar `library_entries`: un mecanismo, no uno por combinación. `relation_type` debe ser enum abierto (adaptación, secuela, spin-off, misma franquicia). Curación manual/comunitaria con cola de revisión, no inferida de APIs.
**Matiz tras 7.34**: la *misma saga* ya NO se modela aquí sino con `sagas`/`saga_items` (agrupación 1→N con vista propia). 8-B queda para relaciones **entre tipos distintos** (adaptación libro↔película/serie), aún sin construir.
**Relacionado — normalización de géneros (pendiente, no resuelta)**: `books.genres`/`movies.genres`/`series.genres` existen pero hoy no se rellenan desde ningún sitio (ni Google Books ni TMDB los capturan en el código). Momento barato para decidir la taxonomía (guardar tal cual y normalizar en lectura, o normalizar al guardar contra un catálogo cerrado) antes de que haya datos con vocabularios distintos. Solo urge si se aborda 7.8 o 7.19.

### 8-C. Separar Obra / Ejemplar / Progreso — *decidido*
`position` nació como "lo único que varía por tipo" para *progreso*, pero en 7.1 se le coló `format` (encuadernación), que es metadato del ejemplar. Se decide separar en **tres capas**: **Obra** (`books`/`movies`/`series`), **Ejemplar** (tu copia concreta — encuadernación, adquisición/precio de 7.29 — en un JSONB nuevo `copy_details` en `library_entries`, separado de `position`) y **Progreso** (`position` + `diary_entries` + futuras `progress_sessions`). Migración de `format` a `copy_details` prevista al retomar 7.1/7.29, no ejecutada aún.
**Actualizado (2026-07-14, ver §7.37)**: `format` se resolvió distinto — no migró a `copy_details`, quedó **sustituido** por la edición del pase (`book_editions`/`movie_versions`): "tapa dura" y "bolsillo" son dos ediciones de la misma obra. `position.format` se conserva solo para leer datos antiguos. `copy_details` sigue sin construirse y sigue siendo la vía correcta para 7.29 (adquisición/precio), ajena a las ediciones.

### 8-D. Infraestructura de notificaciones (compartida por 7.17 y futuras) — *enfoque confirmado*
El proyecto no tiene cron/queue, email ni push. Antes de construir 7.17, decidir una vez la pieza compartida en vez de resolverla feature a feature. A favor de **Web Push**: ya hay un service worker (`public/sw.js`) al que añadir VAPID es extensión natural. Para jobs programados, evaluar primero `pg_cron` (nativo en Supabase). **Matiz (ver 8-F)**: Web Push no funciona en iOS+UE (Apple lo desactivó por la DMA, marzo 2024); fiabilidad iOS solo vía push nativo APNs en un wrapper. No comprometer hasta que 7.17 sea activa; nada "por si acaso".
**Actualización (2026-07-11, EPIC-05)**: el Bloque D del epic social (tabla `notifications` + `NotificationBell`) ya está construido y verificado en dev+prod como notificaciones **in-app puras** — confirma que el núcleo de EPIC-05 (grafo social, clubes, feed) no depende de esta decisión de push ni de 7.31 (Capacitor). La entrega **push** sobre esa misma tabla sigue diferida (E5.D4). Ver SD-5.

### 8-E. Spoiler-safe: utilidad compartida, no cuatro implementaciones distintas — *enfoque confirmado*
7.20 (clubs), 7.21 (comparador), 7.24 (notas ancladas) y 7.30 (modo sin spoilers) necesitan las cuatro "ocultar contenido hasta que el progreso del usuario lo permita". La primera que se aborde debe construirla como **utilidad genérica** reutilizable, no lógica ad-hoc, para que las otras tres la reutilicen.
**Enganche EPIC-05 (2026-07-11)**: el primer consumidor real será **E5.H1b** — el chat por checkpoint de la lectura/visionado conjunto, gateado por el helper `SECURITY DEFINER` `has_reached_checkpoint()`. Bloque H1 aún no está construido; esto es solo el punto de enganche registrado.

### 8-F. ¿Sigue siendo PWA-only la decisión correcta? — *decidido: adoptar Capacitor*
Investigación (julio 2026) sobre las tres cosas que "ir nativo" resolvería: (1) el **widget de pantalla de inicio** sigue sin API web en ninguna plataforma → requiere código nativo sí o sí; (2) envolver la app en **Capacitor** con `server.url` (carga la app Next.js desplegada, con SSR y Server Actions) es un shell delgado aditivo, no una reescritura; (3) **Web Push está roto en iOS+UE** por la DMA, fuera de nuestro control.
**Decidido (2026-07-08)**: adoptar Capacitor **antes** de construir 7.17, precisamente porque Web Push no cubre iOS+UE de todas formas. El widget (punto 3) no se compromete todavía. Restricción de entorno: compilar iOS requiere Xcode/macOS; Android es viable en Windows; el scaffolding de Capacitor es multiplataforma pero iOS necesitará una Mac o runner macOS en CI.

### 8-G. Diario vs. sesiones, y "gestión en la ficha / tarjetas de solo lectura" — *decidido (construido con la base de 7.14)*
Dos decisiones de forma tomadas al construir la base de 7.14: (1) **`diary_entries` y `progress_sessions` son hermanas, no rivales** — pases completos (relectura, con rating+reseña) vs. progreso incremental diario (posición + minutos a mano + nota); no se fusionan, y `library_entries.position` sigue siendo solo el punto actual (sesiones solo para libros y series). (2) **La gestión vive en la ficha del ítem** (`/libro|pelicula|serie/[id]` como hub, `item-manage-panel.tsx`) y las tarjetas de `/u/[username]` se adelgazan a solo-vista; las server actions compartidas migran a `src/lib/library/manage-actions.ts` y `src/lib/diary/actions.ts`; se elimina `ItemLibraryButton`. El dashboard de estadísticas es privado (home), no vitrina pública; objetivos globales por usuario en `profiles`.
**Superseded (2026-07-14, ver §7.37)**: `item-manage-panel.tsx`/`progress-panel.tsx`/`diary-panel.tsx` fueron jubilados y sustituidos por `src/components/detail/log-panel.tsx` (pestaña Registro única que orquesta estado, progreso del pase abierto, sesiones y diario).

### 8-H. RBAC: dónde se aplica el permiso (RLS vs. capa de app) — *decidido (construido en §7.35)*
Los flujos automáticos (búsqueda→catálogo, auto-enriquecimiento de §7.34) y los manuales (añadir ítem, sagas) escriben en las mismas tablas compartidas, y la RLS no puede distinguir "auto" de "manual" del mismo usuario. Se decide mantener la RLS **permisiva** (INSERT para autenticado) y aplicar el gateo de **colaborador** en la **capa de aplicación** (server actions) + ocultando la UI. Lo que sí se blinda en BD es el `role` en `profiles` (trigger anti-escalada + RLS de admin).
**Trade-off**: un usuario técnico podría insertar en el catálogo saltándose la UI (bajo riesgo; catálogo compartido y poco sensible). El endurecimiento duro (enrutar flujos automáticos por un cliente `service_role` y bloquear la RLS de escritura a colaborador+) queda como mejora futura.

## Log de decisiones (fechado)

| Fecha | Decisión | Motivo |
|-------|----------|--------|
| 2026-07-07 | Stack: Next.js 16 (App Router) + TypeScript + Tailwind, Supabase (Postgres+Auth), PWA (no apps nativas) | Simplicidad, un solo codebase, sin gestionar backend propio |
| 2026-07-07 | Metadata en tablas separadas por tipo; **progreso unificado** en `library_entries` con `position` JSONB | Minimizar la deuda al añadir nuevos hobbies (norte del proyecto): añadir un tipo no obliga a replicar tabla de progreso + RLS + query + UI |
| 2026-07-07 | `username` modificable (no fijo), con unicidad garantizada | Flexibilidad para el usuario; el coste de validar unicidad en cada cambio es bajo |
| 2026-07-07 | Perfiles públicos legibles también por visitantes sin cuenta | Coherente con "ver bibliotecas de otros" sin obligar a registrarse para mirar |
| 2026-07-07 | "Añadir manualmente" un ítem es must del MVP | Desacopla la app de que la API externa tenga el título; coste bajo al estar el esquema listo |
| 2026-07-07 | Diario de pases (`diary_entries`) desde el MVP, separado del estado del ítem | Soporta relecturas/re-visionados (estilo Letterboxd); modelarlo después sobre datos reales sería una migración dolorosa |
| 2026-07-07 | i18n cableado desde el inicio (idioma inicial: español) | Evita el refactor tedioso de extraer textos hardcodeados más adelante |
| 2026-07-07 | Catálogo compartido entre usuarios, progreso privado por usuario | Evita duplicar metadatos al buscar el mismo libro/película varias veces |
| 2026-07-07 | Perfiles públicos por defecto, con opción de hacerlos privados | Habilita la función social mínima sin construir el sistema social completo |
| 2026-07-07 | Offline MVP = cache de solo lectura, no offline-first completo | Reduce la complejidad de sincronización manteniendo el beneficio de una PWA instalable |
| 2026-07-08 | MVP cerrado como **v1.0**; §6 completo al 100% | Todas las funcionalidades comprometidas en §4 están construidas y verificadas; ver §10 |
| 2026-07-08 | Adoptar **Capacitor** (wrapper nativo vía `server.url`) antes de construir 7.17; se abandona el PWA-only estricto | Web Push no funciona en iOS+UE (bloqueo de Apple por la DMA, no arreglable en web) — no tiene sentido construir 8-D asumiendo una cobertura que no existe. El widget de pantalla de inicio sigue sin comprometerse (requiere código nativo aparte) |
| 2026-07-09 | Nueva tabla `progress_sessions` (hermana de `diary_entries`) para el progreso incremental diario; la gestión del ítem se mueve a la ficha `/libro\|pelicula\|serie/[id]` y las tarjetas de perfil quedan de solo lectura | Base de 7.14 (rachas/estadísticas): sin ella no se podía reconstruir la actividad día a día; separar "pase completo" de "avance diario" evita sobrecargar `diary_entries`/`position`. Ver §8-G |
| 2026-07-09 | Dashboard de estadísticas **privado** en la home (tira semanal, calendario mensual, rachas, stats anuales) — ruta `/estadisticas` eliminada; objetivos configurables en `profiles` (`daily_goal_minutes`, `annual_goal_items`) | Cierra 7.14: las cuatro piezas de estadísticas sobre `progress_sessions`/`diary_entries`; privado por ser seguimiento personal, no vitrina pública. Ver §8-G |
| 2026-07-09 | Modelo `people`/`credits` (personas + créditos polimórficos) y **tabla `sagas` dedicada** (`sagas`/`saga_items`), no la tabla par-a-par de 8-B | 7.34: fichas de persona (Open Library / TMDB) y sagas con vista propia. Tabla dedicada porque el objetivo era una *vista de saga* (1→N, ordenada); 8-B queda para relaciones entre tipos distintos. Enriquecimiento perezoso (cache-as-you-go) para no reescribir la búsqueda |
| 2026-07-09 | **RBAC** con `profiles.role` (`user`<`collaborator`<`admin`), helpers `SECURITY DEFINER` en RLS y trigger anti-escalada; gateo de colaborador en la **capa de app** | 7.35: colaborador contribuye info manual, admin gestiona roles vía `/admin`. La RLS no puede separar escritura auto vs. manual en tablas compartidas → gateo en server actions; el `role` sí se blinda en BD. Endurecimiento vía service_role, futuro. Ver §8-H |
| 2026-07-09 | Importar biblioteca (7.7) ampliado a **tres** fuentes (Goodreads, Letterboxd `diary.csv`, Bookmory `.xlsx`); entry point en `/u/[username]` (dueño), no en el header global; conversión de rating `rating*2` uniforme a escala 1-10; resolución manual de filas sin match gateada a `collaborator+` | Bookmory y el entry point en el perfil se añadieron a petición explícita del usuario durante la construcción. De paso se corrigieron dos bugs de matching que afectan también a la búsqueda: `isSameTitle` (`title-match.ts`) ganó una guarda de ratio de longitud (≥0.65), y el matching de año (`match-row.ts`) dejó de aceptar candidatos sin año como compatibles salvo que el CSV tampoco tenga año |
| 2026-07-10 | Backfill perezoso de `movies.duration_minutes` y `series.total_episodes`/`total_seasons` (`backfill-queue-sizes.ts`, tope 10 ítems/carga desde `/cola`), extendiendo `ScreenDetails` sobre la llamada a TMDB ya existente | Construyendo 7.22 se vio que ningún código había rellenado nunca estos campos, pese a que 7.8 ya los mostraba. El endpoint de detalles de TMDB ya traía los datos, solo faltaba extraerlos. Corrección permanente del catálogo, útil más allá de la cola |
| 2026-07-10 | Nueva política RLS `UPDATE` permisiva (`USING (true) WITH CHECK (true)`) en `movies` y `series`; `books` se dejó deliberadamente sin ella | Verificando el backfill de 7.22 se descubrió que `movies`/`series` nunca tuvieron política de `UPDATE` (solo `INSERT`/`SELECT`): cualquier `UPDATE` afectaba a 0 filas en silencio. Gap preexistente. Confirmado con el usuario por ser RLS con implicación de seguridad; misma permisividad que el `INSERT`, al ser catálogo compartido no sensible |
| 2026-07-11 | **EPIC-05 Bloque A** (grafo social: seguir usuarios) construido y verificado en dev+prod — tabla `follows` (enum `follow_status`, auto-accept en perfiles públicos / pendiente en privados) y helper `SECURITY DEFINER` `can_view_profile(target_user_id)` que generaliza la RLS de contenido a "público u propio o seguidor aceptado", recableado en `profiles`/`library_entries`/`diary_entries`/`progress_sessions`/`episode_watches`; vista `profile_identities` (solo identidad) | SD-2 de `social-epic.md`: encapsular el `OR` de visibilidad en un único helper anti-recursión (patrón de `has_min_role()`, §8-H) en vez de repetirlo en cada política — la decisión más transversal del epic, previa a feed y clubes. Perfiles privados resueltos como stub de identidad "estilo Instagram" con "Solicitar seguir", no 404 duro. RLS verificada con batería de impersonación (8 casos) antes de prod |
| 2026-07-11 | **EPIC-05 Bloque D** (notificaciones in-app) construido y verificado en dev+prod — tabla `notifications` (enum `follow_request`/`new_follower`/`follow_accepted`, RLS solo-destinatario lee/marca, CHECK `user_id <> actor_id`), dominio `notifications.ts`, componente `NotificationBell` | SD-5 de `social-epic.md`: el núcleo social arranca con notificaciones **in-app** (leídas al cargar), sin depender de push, `pg_cron` ni 7.31 (Capacitor); la entrega push sobre la misma tabla queda diferida (E5.D4). Ver §8-D. Ciclo de los 3 tipos verificado antes de prod |
| 2026-07-11 | **EPIC-05 Bloque B** (reacciones y comentarios en reseñas) construido y verificado en dev+prod — tablas polimórficas `reactions` (UNIQUE para toggle idempotente) y `comments` (hilo plano, sin edición) sobre enum `target_kind` con solo `diary_entry`/`episode_watch` (YAGNI, sin `club_post`), helper `can_view_target()` que delega en `can_view_profile()`; `?tab=` de deep-linking en `item-detail-tabs.tsx` | SD-3 de `social-epic.md`: reacciones/comentarios como dos tablas polimórficas genéricas, sin promover la reseña a tabla propia ("el diario sigue siendo la reseña"). Alcance acotado: sin edición, sin borrado por el dueño del contenido (moderación diferida a E5.J), sin realtime, sin auto-notificación. RLS verificada con batería de impersonación (8 casos) y E2E con dos usuarios antes de prod |
| 2026-07-12 | **EPIC-05 Bloque C** (feed de actividad personal) construido y verificado en dev — **sin migración**: `feed.ts` agrega on-read las cuatro tablas fuente ya existentes filtradas a los seguidos aceptados, normalizando a `FeedEvent` con verbo "el más específico gana"; paginación por cursor. Home gana pestaña "Siguiendo" con fetching condicional por pestaña; `loadMoreFeed` es la primera server action que devuelve datos al cliente | SD-1 de `social-epic.md`: feed on-read puro, cero tablas/RLS nuevas, apoyado en la RLS ya verificada de `can_view_profile`/`can_view_target`. Filtros server-side vía query params (desviación deliberada del boceto cliente-side para no duplicar patrón de filtrado). Revisión final encontró y corrigió dos bugs de integración (misma raíz: `FeedList` no re-derivaba su estado). Verificación manual en navegador por el usuario |
| 2026-07-12 | **EPIC-05 E5.D4** (push, Web Push) construido y verificado en dev (migración también en prod, VAPID pendiente en Vercel) — tabla `push_subscriptions` (enum `channel`, hoy solo `'web'`; `credentials jsonb`; RLS self-only sin UPDATE, resuscribir = delete+insert), `send-push.ts`, `subscription-actions.ts`, `notify()` extendido con `deliverPush()` best-effort, `PushToggle` en la nueva `/cuenta`, listeners en `sw.js` | Cierra E5.D4 (dep §8-D + 7.31): diseño **channel-agnóstico** ("diseñar para ambos, implementar Web Push primero") para que un futuro canal nativo (`ios_native`, necesario para iOS+UE vía APNs/Capacitor) sea un `ALTER TYPE ADD VALUE`. Opt-in explícito vía toggle, nunca prompt automático. VAPID entregadas en chat, nunca commiteadas. Verificación manual por el usuario |
| 2026-07-12 | **EPIC-05 Bloque E** (clubes: creación, membresía y roles) construido, migración en dev+prod, verificación manual pendiente — tablas `clubs`/`club_members` (enum `club_role` en orden ascendente de autoridad, mismo gotcha que `user_role`); cambios de rol como funciones `SECURITY DEFINER` (`create_club`, `set_club_member_role`, `transfer_club_ownership`), no `UPDATE`s de cliente; trigger `reassign_club_ownership` (`AFTER DELETE`) como red de seguridad en BD | Cierra E5.E1–E5.E4 (SD-4): corrección de diseño a mitad — se eliminó el "solicitar unirse" a clubes privados por ser circularmente imposible (un club privado es invisible a no-miembros, así que no podrían ni verlo para solicitar). La Task 1 pasó por 6 rondas de bugs reales (recursión 42P17, escalada de privilegio en accept-invite, conflicto `BEFORE DELETE`/cascade) resueltos con helpers anti-recursión y guards. Feed del club (Bloque F) fuera de alcance |
| 2026-07-13 | **EPIC-05 Bloque F** (feed de club: posts, compartir actividad, encuestas) construido, migración en dev+prod, verificación manual pendiente — tablas `club_posts`/`club_poll_options`/`club_poll_votes`; `target_kind` ampliado con `club_post`/`comment` + CHECK anti-anidación; helper angosto `is_visible_via_club_share()` como `OR` extra (no integrado en `can_view_profile()` para no arriesgarlo); RLS de `club_poll_votes` oculta resultados hasta que votas; `resolveTargetHrefs()` unifica la resolución de enlaces | Cierra E5.F1–E5.F3 (dep E, B, D): "me gusta" en comentarios pasa a toda la app (mismo `target_kind` polimórfico); `activity_share` guarda una **referencia viva** (`{sourceTable, rowId}`), no snapshot, re-derivada en lectura con degradación elegante. Compartir a un club **anula la privacidad de perfil para los compañeros de ese club** (elección de audiencia deliberada, solo ahí). Dos bugs Postgres reales corregidos (`commit;` a mitad de migración por enum; recursión 42P17 en `club_poll_votes`). Motor genérico (Bloque G) fuera de alcance |
| 2026-07-13 | **EPIC-05 Bloque G** (motor genérico de actividades de club) construido, migración en dev+prod, verificación manual pendiente — tablas `club_activities`/`club_activity_participants`/`club_activity_items`/`club_activity_opinions`, enums `activity_kind` (abierto) y `activity_status`; transiciones como tres RPCs `SECURITY DEFINER` (activate/finish/archive), sin política `UPDATE`; helper `is_activity_participant()` gatea pool de ítems y opiniones | Cierra E5.G1–E5.G3 (SD-8, dep E, D): decisión de exponer el ciclo de vida completo ya en este bloque (motor genérico primero, tipos concretos después). `archiveActivity` generaliza "rechazar propuesta" y "cancelar activa" en una RPC. Las opiniones son visibles **solo para participantes** (a nivel de RLS, no solo UI). `config jsonb` y el comportamiento por `kind` diferidos a Bloque H. Batería de 20 checks de impersonación; un hallazgo Important (errores de mutación no visibles) corregido |
| 2026-07-14 | **Registro de pases y ediciones** (§7.37) construido, migraciones en dev (prod pendiente) — `diary_entries` se redefine como el **pase** (`finished_on` nullable = pase abierto), único dueño de nota y reseña, con `is_public`/`edition_id` nuevos; catálogo `book_editions`/`movie_versions` (nunca en series); `progress_sessions` cuelga del pase (`pass_id`); cronómetro persistente en `/sesion/[entryId]`; comunidad agrega desde el **último pase cerrado no abandonado**, no desde `library_entries.rating` (huérfana a propósito con `.notes`) | Cerraba el registro fragmentado (nota/reseña repartidas entre tres campos) y habilitaba dos pases de la misma obra contra ediciones distintas. Se mantuvo el nombre físico `diary_entries` (renombrar habría obligado a tocar RLS/feed/notificaciones sin ganar nada) y se dejaron `library_entries.rating`/`.notes` sin uso para poder revertir. Dos invariantes garantizadas en BD (doble-inserción por doble clic; pase apuntando a edición de otra obra) y dos bugs corregidos (fuga de privacidad de reseñas `is_public=false`; contador de relecturas sumando pases abiertos). Riesgo residual asumido en `register_book_edition` (edición inventada con ISBN válido, firmada y reversible). Ver spec 2026-07-14 |
| 2026-07-14 | **Ediciones en la ficha y editor de ficha oficial** (§7.38) construido en tres fases, migraciones en dev (prod pendiente) — Fase A: el panel de metadatos deja de pintar editorial/ISBN/páginas/idioma de `books`/`movies` como si fueran de la obra; Fase B: ediciones reales de OpenLibrary al abrir la ficha (tope 20, lista negra de editoriales de impresión bajo demanda); Fase C: editor de ficha para colaborador+ (portada, ediciones, sagas; absorbe `saga-assign-form.tsx`) | "Solo colaborador+ edita la ficha" se implementó como **trigger** (`enforce_catalog_edit_collaborator_only`), no policy RLS: una policy filtra filas, no columnas, y con el grant de columna bajo la `UPDATE` permisiva cualquiera habría reescrito título/sinopsis vía REST. Borrar una edición en uso se **impide** (trigger), no se reasigna en silencio. La edición elegida al seguir vive en el **pase**, no en la entrada. Bug corregido: `books` tenía RLS sin policy `UPDATE`, la marca de sincronización se filtraba a cero filas. Ver spec 2026-07-14 |
| 2026-07-19 | **Sagas v2, fase 1 de 4** (§7.4/§7.34) construida y verificada en navegador, migraciones en dev y prod (6 migraciones desplegadas al mergear PR #87) — `sagas` gana `parent_saga_id` (subsagas anidadas reales, trigger anti-ciclos) y `accent_color`; `saga_items` pasa a **multi-membresía** con `is_primary` (índice único parcial); ficha `/saga/[id]` reconstruida con hero+tabs. Tablas `saga_nodes`/`saga_edges` (grafo relacional) y `saga_follows` creadas con RLS pero vacías/sin UI; `assignItemToSaga` deja de borrar membresía previa; `populateTmdbCollection` pasa a diff no destructivo | Ver spec 2026-07-19-sagas-v2-design.md. La subsaga de un nodo del grafo se **derivará** de `saga_items` (una sola fuente de verdad, Info y grafo nunca discrepan); seguir una saga es **solo follow explícito** (`saga_follows`), sin inclusión implícita; el grafo será relacional renderizado con React Flow, no JSONB. Fases 2-4 pendientes |
| 2026-07-19 | **Sagas v2, fase 2 de 4** construida y verificada (QA 8/8, e2e `sagas-v2-mapa.spec.ts` 4/4) — viewer del grafo: pestaña «Mapa de lectura» (condicional a que haya nodos), timeline vertical ramificado en móvil derivado del grafo (`deriveTimeline`), toggle Lectura\|Publicación (`?orden=`), grafo embebido en PC + ruta `/saga/[id]/mapa` con **React Flow** (`@xyflow/react` v12, dependencia nueva); perf de `getSagaDetail` pulido | Ver spec §2.4-§2.5. El canvas se fija a modo oscuro por legibilidad de las aristas coloreadas por subsaga; el timeline móvil se **deriva determinísticamente** del grafo en cada render, no se persiste. Deuda asumida: navegación de nodos solo con puntero (`elementsSelectable=false`). Fases 3 y 4 pendientes |
| 2026-07-19 | **Sagas v2, fase 3 de 4** construida y verificada (QA 10 pasos, e2e `sagas-v2-editor.spec.ts` 3/3, 44 unit) — editor del grafo en `/saga/[id]/mapa/editar`, gate duro collaborator+; lienzo React Flow editable (nodos, aristas tipadas, borrado); panel de añadir título (`ItemPicker`) y subsagas (`SagaPicker`); inspector de nodo (label, subsaga que edita `saga_items`, nivel, `order_no`, conexiones); guardado atómico vía RPC `save_saga_graph`; cierra los DEFERs de fase 2 | Ver spec §3. Guardado con **RPC `SECURITY DEFINER` de full-replace transaccional** (gate de rol también dentro, un reintento tras fallo de red no deja el grafo a medias); las operaciones de membresía van aparte (un reintought del guardado ya es seguro). La subsaga de un nodo se confirma como **dato derivado de `saga_items`**, nunca persistido en el nodo. Cierra el TODO de re-promoción de `is_primary`. Fase 4 pendiente |
| 2026-07-19 | **Sagas v2, fase 3.5 de 5** construida y verificada (e2e `sagas-v2-curacion.spec.ts` 2/2 + QA navegador) — adenda al spec: índice público `/sagas` (buscador, tarjetas raíz con subsagas como chips, «Seguir», «Nueva saga» collaborator+); botones «Editar ficha»/«Anidar en universo» en `/saga/[id]` (`curation-actions.ts`: `createSaga`, `setParentSaga`, `updateSagaMeta`, `uploadSagaCover`); multi-chip con «hacer principal» (`setPrimarySaga`) en el editor de catálogo; enlace «Explorar sagas». Sin migraciones | Ver spec 2026-07-19-sagas-v2-fase-3-5-crear-sagas-design.md. **Sagas nacen también "desde arriba"** (crear el contenedor vacío e ir rellenando). **Universo padre con buscar-o-crear homónimo case-insensitive**: `setParentSaga` reutiliza una saga manual del mismo nombre y devuelve siempre el nombre canónico en BD, no el tecleado. La policy `UPDATE` de `sagas` se deja sin endurecer (la necesita el cache-as-you-go TMDB); el gate collaborator+ va en la capa de server action (`requireCollaborator`), mismo criterio que §8-H. La antigua fase 4 se renumera a fase 5 |
| 2026-07-19 | **Sagas v2, fase 5 de 5** (§7.4) construida y verificada (vitest 58/58, e2e `sagas-v2-biblioteca.spec.ts` 2/2, QA completo) — cierra la spec: tercera subpestaña «Sagas» en Mi Biblioteca, `getFollowedSagas` (consultas planas batched multi-raíz, BFS ≤4 niveles) y `buildLibrarySagaCards` puro (regla de cómputo de §1.5: orden principal, nodos-saga expandidos recursivamente, opcionales fuera, dedup); cards con abanico/tag Grafo, barra segmentada, orden en curso→no empezadas→completadas | Ver spec §4. La tensión §1.5 vs §3.5 se resolvió **admitiendo `order_no` también en nodos-saga** en el inspector (ausencia = rama opcional/spin-off, sin flag aparte). El timeline móvil (`derive-timeline.ts`) queda fuera de este `order_no` nuevo (sigue filtrando solo nodos-ítem). Divergencia aceptada: el hero computa progreso sobre todos los miembros y la card de biblioteca solo sobre el orden principal — no es regresión, es un criterio ya vigente que por primera vez coexiste en pantalla |
| 2026-07-19 | **Sagas v2, mejoras post-v2** construidas y verificadas (e2e curación 2/2 + editor 3/3 + mapa 4/4 + biblioteca 2/2, QA 5/5) — lote de 4 sobre las fases 1–5: fix del descuadre móvil de la cabecera de Info (`flex-wrap`); zona de peligro «Borrar saga» (`deleteSaga`, migración `20260719_sagas_delete_policy.sql`); «Sacar del universo» por subsaga (acción inmediata); aristas flotantes con conexión desde los 4 lados (`ConnectionMode.Loose`, `floating-edge.tsx`) en viewer+editor | Ver spec 2026-07-19-sagas-mejoras-post-v2-design.md. **Borrar una saga no cascada a subsagas** (quedan como raíz vía `on delete set null`) y vale también para sagas TMDB (aceptado que reaparezcan por cache-as-you-go); hasta esta mejora `sagas` no tenía policy `DELETE`, así que el borrado afectaba 0 filas en silencio. El lado de conexión de una arista **no se persiste** (ruta automática). Des-anidar es **acción inmediata y persistida**, no parte del borrador |
| 2026-07-20 | **La regla de cómputo del progreso de saga (§1.5) pasa a ser ÚNICA**, en `src/lib/sagas/main-order.ts`, y la consumen hero y cards (PR #97, issue #91) | **Deroga la «divergencia aceptada» de la entrada del 2026-07-19** (fase 5), que daba por bueno que el hero contara todos los miembros del subárbol y la card solo el orden principal. Coexistiendo en pantalla no era un criterio, era un descuadre: sobre la misma saga el hero decía 2/7 = 29% y la card 2/5 = 40%. Medido en navegador antes y después. `get-saga-detail` pasa a pedir los nodos de todo el subárbol (`.in` en vez de `.eq`) para poder expandir los nodos-saga; sin viaje extra |
| 2026-07-20 | **Onboarding de 3 pasos + bienvenida** para quien se registra (PR #100): intereses → primeros títulos → gente y clubes. Migración `20260720_onboarding.sql` (`profiles.interests`, `profiles.onboarded_at`), aplicada en prod | Antes no había onboarding: `/onboarding` creaba el perfil y redirigía, así que el recorrido real era «confirmar email → home vacía». **Los intereses se persisten y se consumen** (tipo por defecto en Buscar, filtro de «Todo» en Colección): preguntar algo que no cambia nada es decoración. El paso de gente **se omite solo** mientras no haya a quién sugerir (hoy 6 perfiles y 2 clubes en prod). Al implementarlo se descubrió que `proxy.ts`, `AppShell` y la acción de registro usaban «tiene perfil» como sinónimo de «está onboardeado» — cierto cuando el onboarding ERA elegir el @usuario, y por eso el asistente no se le habría mostrado a nadie |
| 2026-07-20 | **Importar biblioteca reducido a dos fuentes**: se retira **Bookmory** (`.xlsx`), quedan Goodreads y Letterboxd, ambas CSV (PR #101) | **Deroga la ampliación a tres fuentes del 2026-07-09.** Petición del usuario al plantear un paso de importación en el onboarding: menos superficie que sostener y que explicar en la primera pantalla. Bookmory era la única fuente en hoja de cálculo y sostenía **sola** la dependencia `exceljs`, que se elimina con ella (**90 paquetes menos**, 636 → 546) — la misma que `TRAMPAS.md` registraba como causa de un `next build` roto. Al quedar solo CSV, `detectFormat` devuelve el formato directamente en vez del envoltorio `FormatDetection`/`kind` |
| 2026-07-20 | **El parser de Letterboxd agrupa los visionados por película** (título+año) en vez de emitir una fila por visionado (PR #102) | Corrección de **pérdida de datos**, no optimización: `diary.csv` trae una fila por visionado, pero `commit-row.ts` espera una fila por obra con todas sus fechas. Sin agrupar, el segundo visionado chocaba con `passes_one_active` y su fecha se descartaba en `addHistoricalPasses` — **los revisionados desaparecían** y se reportaban como «duplicado». Segundo camino del mismo fallo: el `skip` de la fecha activa se aplicaba aunque el pase ya existiera de antes, así que al REIMPORTAR se perdía el visionado nuevo; ahora solo se salta cuando el pase se acaba de crear con esa fecha. De paso el matching se hace una vez por película, no por visionado |
| 2026-07-20 | **Las colas (§7.22) se retiran y su papel pasa a las colecciones marcadas `is_sorteable`**: la UI de Colas se borra (5 componentes), el sorteo (§7.28) gana un filtro por colección y la tabla `queues` queda sin uso a la espera de su `DROP`. Migración `20260720_collections_sorteable.sql` | **Deroga la P3 del plan 02** («Colas se mantiene como pestaña»), que ya era falsa en el código: al integrar Colección v2, `colas` salió de las subpestañas visibles y **ningún enlace llevaba a `?tab=colas`** — la pantalla llevaba semanas inalcanzable y nadie lo notó porque el e2e navegaba por URL directa. Colecciones v2 ya cubría «lista nombrada y ordenada» (`collection_items.position`), así que mantener dos conceptos solo confundía. El filtro es **opt-in por colección** a petición del usuario: con 19 colecciones, ofrecerlas todas lo hacía inservible. Se descartó convertir las colas existentes en colecciones al comprobar que **las 3 de prod están vacías** (0 pases con `queue_id`): habría creado colecciones vacías, no rescatado trabajo. Las estimaciones de tiempo por ritmo personal se retiran de pantalla pero **los helpers siguen vivos dentro del sorteo**, porque su filtro de duración depende de ellos en libros. Hallazgo al ejecutar: `backfillQueueSizes` —lo único que rellena `movies.duration_minutes` y `series.total_episodes`— **solo se invocaba desde el panel de Colas**, así que llevaba sin ejecutarse desde que quedó huérfano; se muda a `getSorteoPool` |
| 2026-07-20 | **Fase B de la retirada de colas: se borra el modelo** (`20260720_drop_queues.sql`: tabla `queues`, columnas `queue_id`/`queue_order` en `passes` y `library_entries`, RPC `reorder_queue`), el CRUD (`app/coleccion/actions.ts`), el namespace i18n `queue` y los tres módulos de cola. Los helpers de ritmo sobreviven en **`src/lib/pace/`** (`QueueItem`→`EstimableItem`, sin los campos muertos `queueId`/`queueOrder`) porque el filtro de duración del sorteo los necesita | **Hallazgo: la fase A dejó el sistema a medias y no lo vi al cerrarla.** Se retiró el *visor* de colas pero quedó vivo el *asignador*: las tres fichas seguían ofreciendo «añadir a cola» y escribiendo `queue_id`, así que se podía meter un ítem en una cola invisible. Regla que se generaliza: **al retirar una feature, buscar sus escrituras además de su pantalla** — el grep de «¿quién enlaza aquí?» encuentra el visor, no el productor de datos. **Orden de despliegue obligatorio: código primero, `DROP` después.** El `DROP` se aplicó solo en dev a propósito: producción sirve todavía el código con `getQueues()` en las tres fichas, y borrar la tabla antes de desplegar rompería `/libro`, `/pelicula` y `/serie` |
| 2026-07-20 | **Registrar sesión pasa de página completa a modal por ruta interceptada** (`@modal` + `(.)sesion/[passId]`), y `addSession` **deja de hacer `redirect()`** y devuelve estado. La hoja se rediseña con hero, cabecera y footer pegajosos; el bloque de libro gana rail de tres tramos y stepper cuya **cifra es un input** (con chips +10/+25/+50); el de serie, rejilla de episodios con miniatura, tres estados y **altura tope con scroll propio**. La nota sale de la hoja (el servidor sigue aceptándola). Sin migraciones. Ver spec 2026-07-20-registrar-sesion-v2-design.md | A `/sesion/[passId]` se llegaba desde **cinco** pantallas y al guardar redirigía siempre a la ficha: registrabas desde el inicio y acababas en otro sitio. **Un `redirect()` de servidor es incompatible con un modal** — por eso el cambio de fondo está en la server action, no en la UI. La ruta interceptada permite no tocar ninguno de los cinco `<Link>` y conservar el deep link y la recarga como página entera. **Patrón a reutilizar en cualquier hoja con varios puntos de entrada.** Tres bugs reales que solo salieron al verificar en navegador (lint, tsc y build los daban por buenos): (1) el `<dialog>` exterior y `ClosePassSheet` disparaban **cada uno su `router.back()`** y te dejaban en el inicio en vez de en el origen — resuelto con un `closeOnce()` guardado por ref al que embocan todas las salidas; (2) el `<dialog>` recorta con `overflow-hidden` y la hoja no tenía scroll interno: **184 px de contenido inalcanzables** a 390×560; (3) `SessionHero` lleva `overflow-hidden` para el tinte, lo que **anula su tamaño mínimo por contenido** en flexbox, así que se comprimía a 93 px y la portada de 104 px se solapaba con el campo de abajo (`shrink-0`). Regla que se generaliza: **el anidamiento de `<dialog>` y el box model dentro de un modal hay que medirlos en navegador, no razonarlos** |
| 2026-07-21 | **Notas y citas, Plan A** (§7.24/§7.27 texto; OCR queda fuera) construido y verificado (e2e `notas` de captura, anclaje vivo y orden) — `NoteComposer` sustituye a `AddNoteForm` en la ficha y se monta también en la hoja de registrar sesión, con el anclaje siguiendo en vivo a la página/episodio que marcas; lista "Mis notas y citas" en la ficha ordenada por posición (`src/lib/notes/sort.ts`); migración `20260721_notes_social_columns.sql` añade a `public.notes` `meta jsonb`, `is_spoiler`, `is_public`, `parent_note_id` + índices `idx_notes_item`/`idx_notes_parent`, aplicada en dev y prod. De paso, dos defectos preexistentes corregidos: el tipo `Note` tiraba en silencio las posiciones de serie (solo leía `page`), y `deleteNote`/`toggleNoteFavorite` no revalidaban la ficha (misma clase de bug que #36/#37/#39/#66) | Dos decisiones de forma que un futuro repaso no debería tener que re-descubrir mirando el código: (1) **`is_public` se escribe sin política de lectura pública.** La columna registra la intención de compartir del usuario, pero abrir una policy de lectura ahora —antes de que exista el filtro spoiler-safe (§8-E)— sería exactamente la fuga de datos privados que la columna venía a evitar. Las 4 políticas de dueño de `notes` siguen siendo las únicas (confirmado contra `pg_policies` en prod); la lectura se abre en el ciclo del muro público. (2) **El orden por posición no reimplementa la comparación.** La spec de diseño proponía una función nueva `positionSortKey`; se descartó porque habría sido una tercera implementación de la misma lógica de comparar posiciones — la que ya vive en `comparePositions` (`src/lib/library/position.ts`) y que ya está duplicada a propósito en SQL para los checkpoints de clubes (§8-E). `src/lib/notes/sort.ts` (`compareNotes`) delega en `comparePositions` en vez de sumar una tercera copia |
| 2026-07-21 | **Notas y citas, Plan B: el cuaderno `/notas`** construido y verificado (e2e `notas-cuaderno`: filtros, búsqueda, orden por obra, paginación y medida a 390×700). Ruta propia y privada del dueño, con filtros (obra, tipo, etiqueta, favoritas), búsqueda sobre `body` y paginación **en SQL**; el estado vive entero en la URL (`src/lib/notes/query.ts`, puro y probado). `getNotes` se parte en `getNotesPage` (filtros + `range` + `count` exacto), `getNoteCounts` (tres `count` en SQL) y `getNotesForSorteo` (acotada a 60). **Sin migración: Plan B no toca el esquema ni RLS.** | Dos decisiones de forma. (1) **«Por posición» no puede ser un orden global, y «por obra» no se puede hacer en SQL.** La spec §4 pedía tres órdenes (recientes · por obra · por posición); comparar «Pág. 12» de un libro con «T1·E3» de una serie es comparar dos escalas distintas —por eso `compareNotes` exige un `itemType`: ese orden solo existe *dentro* de una obra— y ordenar por título es imposible desde PostgREST porque el título vive en `books`/`movies`/`series` según `item_type`. Los tres se funden en **dos**: `recientes` (exacto en SQL) y `obra`, que ordena por `(item_type, item_id, created_at)` —arbitrario respecto al título pero **estable**, que es lo que la paginación necesita para no repetir ni saltarse filas— y pinta la página agrupada por obra, con `compareNotes` ordenando dentro de cada grupo. (2) **Los contadores del Rincón dejan de derivarse del array completo.** `RinconTab` hacía `getNotes()` sin límite y se llevaba todas las notas al cliente solo para que Memorizar eligiera una al azar y para contar citas/notas/favoritas; ahora el sorteo recibe una muestra acotada y los contadores son `count` en SQL. Es el mismo patrón que el cuaderno viene a establecer: si una pantalla necesita todas las filas, es que le falta paginar |
| 2026-07-21 | **El feed deja de servir `progress_sessions.note`**: se quita del `select` y del tipo `FeedEvent.progress` (también en `shared-activity.ts`, que resuelve lo compartido a un club). Vigilado por `e2e/feed-nota-privada.spec.ts`, que mira el HTML **con el payload RSC incluido**, no lo pintado | El texto de una nota de sesión se escribe en dos sitios (ver la entrada de Plan A): la fila de `notes`, **privada del dueño** —sin política de lectura pública, `is_public` escrito pero no honrado— y la columna vieja `progress_sessions.note`, que el feed servía a tus seguidores. La misma frase salía por la otra puerta, **y por defecto**, porque la casilla «Compartible» viene desmarcada y el compositor promete literalmente «por ahora nadie más la ve». Nadie la pintaba (`feed-card` solo usa los minutos), pero `FeedCard` es un componente de cliente: el texto viajaba **serializado en la carga de la página** de cualquier seguidor, legible en el HTML. Por eso el test no puede ser visual — un aserto sobre lo que se ve pasaba con la fuga presente. La columna se sigue escribiendo (alimenta el recuento de `get-today-focus`); lo que se corta es servirla. Cuando exista el muro público con filtro spoiler-safe, lo que se sirva será la fila de `notes` que el usuario haya marcado, no esta columna |
| 2026-07-21 | **El estado optimista de la ficha publica también si está guardando** (`isSaving` en `ItemStatusContext` → `aria-busy` en el badge, en sus dos caras) y **`addExistingItemToLibrary` revalida `/coleccion`**. Cierra la issue #106 | El diagnóstico anterior (`docs/TRAMPAS.md` §16: «la navegación aborta la server action, el pase no se crea») era **falso**: instrumentada la acción, se ejecuta siempre, el plan es `createActive` y el insert devuelve fila con `error: null`. Lo que fallaba era el ORDEN — la consulta de `/coleccion` caía entre la entrada de la acción y su insert. La causa raíz es que **el optimismo se comía su propia señal**: `useFollow` publica el estado en el mismo tick en que dispara la acción, y eso desmonta el botón «Seguir» que era el dueño del `isPending`, así que nada en el DOM decía que la escritura seguía en vuelo y ni el usuario ni un test podían esperarla. Lección general para cualquier UI optimista de este repo: **si publicas un estado antes de tiempo, publica también cuándo deja de ser provisional**, o dejas al usuario sin forma de distinguir "guardado" de "guardándose". Y una segunda: `revalidateLibrary()` existía y no se llamaba — el módulo `reactivity/revalidate.ts` se creó justamente para que "olvidar una ruta" no fuera posible, y aun así se olvidó |
| 2026-07-21 | **La salida del modal de registrar sesión deja de depender del historial**: `SessionModal` cierra el `<dialog>` a mano y vuelve a la ficha con `back()` solo si la entrada anterior ES la ficha, y con `replace(exitHref)` si no. `exitHref` (`/libro/<id>?tab=log`) lo calcula el server component de la ruta interceptada. Regresión vigilada en `pase-hub` por una aserción de **URL** en las dos salidas del modal (Regla 2 = con hoja de cierre; «abandonar y retomar» = sin ella). Sin migraciones. Cierra la issue #117 | **Deroga el «un solo salto de historial» de la entrada del 2026-07-20** (registrar sesión v2), que daba por bueno que `back()` fuera un destino. No lo es: Next solo crea entrada nueva si su `pushRef.pendingPush` sobrevive hasta el efecto que sincroniza el historial, y en la ficha hay actualizaciones del router en vuelo constantemente (cambiar de estado dispara una server action que revalida). Medido con la Navigation API: la ida al modal sale como `navigate type=replace` y `entries()` pasa de `[…, /libro/X]` a `[…, /sesion/Y]` — **la entrada de la ficha desaparece**, así que `back()` saltaba a lo que hubiera antes (en el e2e, el inicio). Dos callejones sin salida que conviene no repetir: (1) **quitar el `router.refresh()` del cambio de estado NO lo arregla** (probado) — la revalidación de la propia server action ya basta para pisar `pendingPush`; (2) **`replace` a secas tampoco**, porque en navegación *soft* Next **conserva el slot paralelo** `@modal` (su `default.tsx` solo entra en navegación dura) y el `<dialog>` se queda abierto encima de la ficha — `back()` estaba haciendo **dos** trabajos, volver *y* desmontar el modal, y solo se veía el primero. De ahí que el cierre del `<dialog>` sea ahora explícito. Límite asumido y anotado en el código: la comprobación de «¿la anterior es la ficha?» usa la **Navigation API** (Chromium); donde no exista responde que sí y el comportamiento es el de antes — el helper solo puede mejorar el destino, nunca empeorarlo |
| 2026-07-21 | **El trigger `promote_active_pass_after_delete` se aplica a dev**, que llevaba sin él desde su migración (`20260719_`). Cierra la issue #118 | Salió al hacer el baseline de #117: la «Regla 6» de `pase-hub` fallaba **también sobre `main`**, y el snapshot mostraba la ficha con el botón «Seguir» — o sea, borrar el pase activo **esfumaba la obra de la biblioteca**, justo la regresión que ese test vigila. La causa no estaba en `deletePass` (un `DELETE` pelado, sin promover nada) sino en que el arreglo vive en un **trigger** que estaba en prod y no en dev. Comprobado contra `pg_proc`/`pg_trigger`, no contra `list_migrations` (misma regla de `AGENTS.md` que ya nos ahorró un falso negativo). **Producción nunca estuvo afectada**; el síntoma era un e2e que falla solo en local, que es de lo más fácil de archivar como *flaky*. Es la regla «dev primero, luego prod» aplicada del revés, y conviene un repaso de simetría dev/prod: si esta migración se saltó dev, es poco probable que sea la única |
| 2026-07-21 | **Se cierra la retirada de colas aplicando el `DROP` a prod** (`20260720_drop_queues.sql`: tabla `queues`, `queue_id`/`queue_order` de `passes` y `library_entries`, RPC `reorder_queue`), y **`DRIFT-CHECK.md` gana una quinta superficie: entornos dev↔prod**, con las consultas normalizadas del barrido. Cierra las issues #121 y #122 | La fase B llevaba un día **a medias a propósito** —el `DROP` solo en dev, esperando a que producción dejara de servir `getQueues()`— y ese pendiente solo vivía en una fila de este registro. Confirmado antes de borrar: cero referencias a colas en `src/`, despliegue de producción en `b491279`, y las 3 colas de prod **vacías** (0 pases y 0 entradas con `queue_id`; nombres «Cera», «hokum», «kjnlnjk», creadas por el mismo usuario en 41 segundos — datos de prueba). Tras aplicarlo, **dev y prod quedan con digests idénticos** en las cinco superficies (`columns` 45, `func` 57, `index` 116, `policy` 124, `trigger` 25). **Lección de método, que es lo que se lleva a `DRIFT-CHECK.md`: comparar `md5(prosrc)` en crudo es inservible.** Dio 6 funciones «distintas» que eran idénticas en lógica — una difería solo en **CRLF vs LF** (aplicada desde Windows en un entorno y desde otro sitio en el otro) y el resto en **comentarios**. Ese ruido casi entierra la única deriva real del barrido: la policy `club_activity_items insert participant or curator`, que en dev conservaba la versión anterior a `20260715_consolidate_activity_items_policies.sql` (sin la rama `tierlist`). Regla nueva: **normalizar siempre** —quitar comentarios, colapsar espacios— antes de declarar una diferencia |
| 2026-07-21 | **La vista de actividad de club en PC entra en `ClubShell`** (mismo sidebar del club que el resto de pantallas del club) **y el responsive se resuelve con un layout de tres ranuras — `railTop`/`body`/`railBottom` — y un solo DOM**, sin duplicar controles por breakpoint (`ActivityLayout`, `src/components/clubs/activity-layout.tsx`); el orden del DOM es el orden móvil, y en `lg` un grid explícito recoloca las ranuras. Sin migraciones. Spec `docs/superpowers/specs/2026-07-21-actividad-club-pc-design.md`, plan `docs/superpowers/plans/2026-07-21-actividad-club-pc.md` | **Motivo de «un solo DOM»**: duplicar el mismo control por breakpoint (uno visible en móvil, otro en PC, alternando con `hidden`/`lg:hidden`) rompe los locators de la suite e2e, que corre a 1280 — `getByRole` encuentra las dos copias y Playwright lanza una violación de *strict mode*. La única excepción admitida es el bloque de participantes en el rail (`railExtra`), legítima por tratarse de avatares no interactivos (sin `role` que colisione), y va **condicionada a que la actividad monte tablero** (`hasBoard`): si no hay tablero no hay rail que lo sustituya, y el bloque se queda en su sitio del orden móvil. `Layout` se pasa siempre como la referencia importada `ActivityLayout`, nunca como un wrapper inline creado en el render: un wrapper nuevo en cada render cambia de identidad y React remonta el subárbol — se cierra el `<details>` de la matriz de `list_challenge` y se pierde el texto a medio escribir en el chat de un hito de `buddy_read`. Los tableros (`tierlist-board.tsx`, `criteria-challenge-board.tsx`, `list-challenge-board.tsx`, `buddy-read-checkpoints.tsx`) no se parten: reciben `Layout` como prop y reparten su propio JSX entre las ranuras, conservando su fetch único |
| 2026-07-21 | **Revisión final de la vista de actividad en PC** (misma feature de la fila anterior): (1) los cuatro early returns con contenido de los tableros (pool vacío/no-participante de `list_challenge`, no-participante/sin config de `tierlist` y `criteria_challenge`) pasan a devolver `<Layout railExtra={…} body={…} />` en vez de un `<div>` suelto — sin eso, el bloque de participantes desaparecía a 1280 mientras `view` es `null` (todas las cargas) y en los estados degenerados; los que devolvían `null` (nada que enseñar) se quedan en `null`. (2) `hasBoard` en `activity-detail.tsx` pasa a montar el tablero de `list_challenge` sin gatear a `isParticipant`, igual que ya hacía `buddy_read` — sin esto, `LinkedActivities` (que el club entero debe poder ver, `linked-activities.tsx:12`) quedaba detrás del gate de participante, y un moderador/creador que no se había unido a un reto ya `finished` perdía su único botón para «Cerrar con tierlist». (3) `ActivityLayout` deja de renderizar un ítem flex vacío en móvil cuando el rail-arriba solo trae `railExtra` (oculto por CSS en ese breakpoint) sin `railTop` — el contenedor pasa a `hidden lg:flex` en ese caso. (4) `railBottom` de `buddy-read-checkpoints.tsx` («Próximo hito») se gatea a `viewerIsParticipant`, igual que `railTop` («Tu progreso»): el `status` de un checkpoint es por viewer, así que sin el gate un no-participante veía un «Próximo hito» que no le decía nada | Los cuatro puntos salieron de una revisión de rama, no de un reporte de usuario. Verificado con un caso real (no solo lectura de código): un moderador NO participante en un `list_challenge` `finished` sembrado por REST sí ve «Cierra con una tierlist» al navegar. Un hallazgo relacionado se dejó **sin arreglar a propósito**: si `LinkedActivities` no tiene nada que pintar (activo, sin hijas, sin oferta) dentro del `railBottom` de un early return de `list_challenge`, el contenedor del rail igual se monta vacío — variante menor del mismo patrón que el punto (3) sí corrige, pero aquí el contenido depende de datos, no de breakpoint, y closearlo exigiría duplicar en el tablero la lógica de visibilidad de `LinkedActivities` (o exportarla). No se hizo por ese motivo; queda para quien lo note en pantalla |
| 2026-07-21 | **Se revierte el punto (2) de la entrada anterior**: `hasBoard` en `activity-detail.tsx` vuelve a ser estrecho (`(isParticipant \|\| kind === "buddy_read") && Boolean(DetailExtension)`), `list_challenge` deja de montar `ListChallengeBoard` para quien no participa, y `LinkedActivities` vuelve a vivir como hermano de `structureSection` en la vista principal (como en `main`), gateada a `activity.kind === "list_challenge" && !isParticipant` para no duplicarse con la copia que el tablero ya pinta en su `railBottom` cuando el viewer SÍ participa. Además: `ActivityLayout` gana `empty:hidden` en los dos contenedores de rail (resuelve estructuralmente el hallazgo "sin arreglar a propósito" de la entrada anterior); los early returns de `null` de `list-challenge-board.tsx` (`!view`), `buddy-read-checkpoints.tsx` (`!view \|\| !view.itemType`) y `tierlist-board.tsx` (`!board`) pasan a `<Layout railExtra={railExtra} body={null} />` para que el bloque de participantes no desaparezca durante la carga en PC; y los early returns `!activity.viewerIsParticipant`, ya inalcanzables antes de esta rama, de `tierlist-board.tsx` y `criteria-challenge-board.tsx` ganan un comentario que lo explica en vez de borrarse (defensa en profundidad) | El ensanchamiento de `hasBoard` "funcionaba" pero tenía un coste no visto en la revisión anterior: un no-participante de un `list_challenge` **activo** veía tres invitaciones a unirse apiladas (rejilla de preview + mensaje propio del tablero «Únete al reto para ver el progreso…» + el teaser con su botón real), y en un reto **`finished`** el mensaje del tablero decía «Únete» cuando el botón «Unirme» ni se pinta (solo con `status === "active"`). Se prefiere que `ListChallengeBoard` no se monte en absoluto para un no-participante — como ya pasaba en `main` antes de toda esta feature — y que `LinkedActivities`, al no depender de datos del tablero (solo de `activity.linkedChildren` y `activity.status`), se pinte directamente sin pasar por el rail. Verificado el caso que motivó todo esto (moderador/creador NO participante, reto `finished`) con siembra REST + navegación real: sigue viendo «Cierra con una tierlist», ahora sin las otras dos invitaciones a unirse. Issue abierta para el hueco de cobertura que dejó pasar la regresión: `e2e/club-activity-changes.spec.ts` comprobaba el teaser pero no la ausencia del mensaje propio del tablero (#128) |
| 2026-07-22 | **Eventos de club** (§ actividades) construidos y verificados — kind nuevo `evento` en `activity_kind` (migraciones `20260722_activity_kind_evento.sql` y `20260722_club_event_rpcs.sql`, dev; **prod pendiente**, reservado al usuario): actividad NO participativa con solo fecha, título y descripción, creada por moderador+ vía `create_club_event`, sin vista individual. `ActivityKindDefinition` gana `hasDetailView`; helpers puros `groupActivities` y `format-date` extraídos con tests | Ver spec 2026-07-22-club-eventos-design.md. **Kind nuevo en vez de tabla aparte, aplicando SD-8**: el calendario que viene debe leer UNA fuente de fechas de club, no un `UNION` con RLS y notificaciones duplicadas; el precio asumido son las columnas sin uso (`config`, `ends_on`, satélites vacíos) en las filas de evento. El estado **«pasado» se DERIVA** de `starts_on < today` al leer y nunca se persiste — un estado sincronizado a mano con el calendario se desincroniza. `update_club_event` lleva `and kind = 'evento'` **obligatorio**: sin él, una RPC `SECURITY DEFINER` gateada solo por rol reabre la edición arbitraria de las otras cuatro kinds que SD-8 evitó al no crear la política `UPDATE`. **Añadido durante la implementación, fuera de la spec original**: `update_club_event` exige además `status = 'active'` — un evento archivado no puede reescribirse, mismo criterio que las RPC hermanas de `club_activities`, todas gateadas por estado. `hasDetailView` se hizo **requerido y no opcional con default**, para que el próximo kind tenga que decidirlo en vez de heredarlo. Al implementarlo se descubrió que `club-summary.tsx` metía los eventos en el strip de «en marcha» (comparten `status='active'`) con enlace a una ficha que da 404 — corregido con el mismo filtro que la lista. El e2e de creación/edición/archivado con moderador real y el de miembro raso sin la tarjeta «Evento» **sí se construyeron** (`e2e/club-evento.spec.ts`) — la premisa de que "solo hay una cuenta de prueba seedeada" que motivó dudar de su viabilidad **era falsa**: varios specs ya creaban usuarios ad hoc vía API admin. Queda como issue abierta solo el camino negativo de llamar a la RPC directamente como miembro raso (sin pasar por la UI) |
| 2026-07-22 | **Eventos de club gana un `target_type` polimórfico nuevo, `club_event`**, distinto de `club_activity`, para la notificación `club_event_created` (`notify-club.ts`, `notification-types.ts`, `resolveTargetHrefs` en `notifications.ts`) | Revisión final de rama, añadido a la entrada de la fila anterior sin reescribirla (**deja constancia de una decisión de forma que esa entrada omitió**). Un evento es físicamente una fila de `club_activities` — no hacía falta `target_kind` nuevo en BD, `club_event` vive solo en la capa de tipos de la app — pero su notificación no puede resolverse como una `club_activity` normal: esa rama de `resolveTargetHrefs` enlaza a `/club/[slug]/actividad/[id]`, la misma URL que la guardia de ruta de `hasDetailView: false` hace devolver 404 a propósito. Sin el `target_type` distinto, notificar la creación de un evento habría producido una notificación real que lleva a un 404 real. `club_event` resuelve en cambio a `/club/[slug]` (la ficha del club, donde el evento sí es visible en «Fechas señaladas»). Mismo criterio que ya obligó a filtrar eventos del feed global (`club-feed.ts`) y del strip de «en marcha» (`club-summary.tsx`): **cualquier superficie que construya su propio enlace a la ficha de actividad tiene que excluir o redirigir `evento` aparte**, porque no comparte su suposición de base (que todo `kind` tiene página propia) |

> Nota histórica (heredada de la versión previa de este registro): la entrada del 2026-07-10 sobre la política `UPDATE` permisiva en `movies`/`series` (creada al construir 7.22) quedó **superada** ese mismo día por un endurecimiento RLS del catálogo (quitar el `UPDATE` abierto al rol público, acotarlo con grants de columna, `collaborator+` para editar `sagas`/borrar `saga_items`, FK compuesto `(library_entry_id, user_id)` en `diary_entries`/`progress_sessions`). Ver §8-H y 7.22 en el backlog para la trazabilidad. **Esta fila de endurecimiento no figura en la tabla §9 de `REQUIREMENTS.md`** (ver notas de contradicción al pie).
