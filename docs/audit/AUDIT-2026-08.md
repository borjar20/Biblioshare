# Auditoría integral — agosto 2026

> **[Vivo · fase 1 completada el 2026-08-19 · fase 3 (UI/UX desktop) completada el 2026-08-19]**
>
> Auditoría por fases. Esta primera pasada cubre arquitectura, funcionalidad existente,
> modelo de datos, calidad estructural, deuda técnica y código obsoleto. **Solo diagnóstico:
> no se aplicó ningún cambio de código.** Fases posteriores decidirán qué se arregla y en qué orden.
>
> Método: 6 pasadas paralelas de agentes de solo lectura (rutas/app, dominio `src/lib`, modelo de
> datos contra BD dev real vía MCP, código muerto, funcional por dominios, caché/RLS/rendimiento),
> partiendo de `docs/architecture/graph.json` y los docs canónicos. Rama auditada:
> `fix/674-catalogo-server-authoritative` (los hallazgos afectados por merges recientes se
> re-verificaron contra `main`; se señala cuando aplica).
>
> Cifras del repo en el momento de la pasada: ~1.004 ficheros TS/TSX y ~111k LOC en `src/`,
> 176 tests unitarios, 82 specs e2e, 187 migraciones locales, 55 tablas en dev (todas con RLS),
> 125 funciones en BD, 29 enums.

## Fase 1 — Arquitectura y funcionalidad

Convenciones: cada hallazgo lleva ID `F1-###`, área (`arquitectura` | `funcional` | `datos` |
`cache-rendimiento` | `obsoleto` | `doc`), tipo (`BUG` | `RIESGO` | `DEUDA` | `LEGACY` |
`MEJORA`), prioridad P0–P3 (criterio del repo: daño a quien usa la app) y confianza
(alta | media | baja). Lo ya rastreado en issues abiertas NO se repite como hallazgo; se lista
como contexto en §C.

### Bugs

---

**F1-001 · funcional · BUG · P1 · confianza alta**
**Las reseñas de pase de una serie se escriben pero no se muestran nunca.**
`src/components/detail/community-panel.tsx:46-66` elige rama por `episodeReviews !== undefined`,
y `src/app/serie/[id]/page.tsx:547` pasa SIEMPRE el array (aunque esté vacío), así que la rama de
reseñas de pase jamás se pinta. Una reseña escrita al cerrar el pase de una serie (`ClosePassSheet`,
pública por defecto) no aparece en la ficha, que dice «Todavía no hay reseñas»; además la página
paga `getReviews` (`serie/[id]/page.tsx:431`) para descartar el resultado. Libro y película sí las
muestran. Verificado también en `main`. Relacionada con #556 pero no cubierta por ella.
*Impacto:* contenido del usuario desaparece en silencio; asimetría no intencionada entre tipos.
*Recomendación:* pasar `episodeReviews` solo cuando aplique, o fusionar ambas fuentes en el panel.

---

**F1-002 · funcional · BUG · P1 · confianza alta**
**El importador crea pases `completed`/`dropped` sin fechas y rompe el invariante
«`finished_on IS NULL` ⟺ pase abierto».**
`src/lib/import/commit-row.ts:60-78` inserta el `status` crudo del CSV sin pasar por
`planTransition`; con *Date Read* vacía en Goodreads (`parse-goodreads.ts:68`) nace un `completed`
con `started_on`/`finished_on` nulos. El gemelo SQL `resolve_pending_import`
(`supabase/migrations/20260717_pass_hub_c_rename.sql:208-241`, SECURITY DEFINER) tiene el mismo
hueco. Consecuencia: `log-panel.tsx:288` (`openPass = finishedOn === null`) y
`get-passes.ts:25` tratan ese pase como lectura EN CURSO; al releer la obra quedan dos pases
«abiertos de facto». No existe CHECK ni trigger que ligue estado y fechas.
*Impacto:* estados imposibles persistentes que contaminan biblioteca, diario y estadísticas.
*Recomendación:* normalizar fechas en el import (TS y SQL) + CHECK en `passes` que ligue
`status` con `finished_on`.

---

**F1-003 · datos · BUG · P1 · confianza alta**
**Triggers vivos en BD siguen escribiendo `status` en `library_entries` (tabla CONGELADA).**
`public.autoadd_library_on_activity_item` y `public.autoadd_library_on_activity_join` hacen
`insert into library_entries (…, status) values (…, 'planned')` y están activos vía
`trg_autoadd_library_on_activity_item` (sobre `club_activity_items`) y
`trg_autoadd_library_on_activity_join` (sobre `club_activity_participants`). Verificado con
`pg_get_functiondef`/`pg_trigger` en dev. Nunca crean un pase, así que el «auto-añadir a
biblioteca» al unirse a una actividad de club escribe en la tabla muerta y **no hace nada
visible**: la obra no aparece en la pila real del usuario (todo deriva de `passes`).
Cuarto episodio de la serie `library_entries` (PR #96, #470, #674).
*Impacto:* feature inerte + escritor activo de la tabla congelada, cebo para el próximo bug.
*Recomendación:* decidir — o los triggers crean un pase `planned` vía la vía canónica, o se
eliminan y se registra acta. Verificar también en prod.

---

**F1-004 · funcional · BUG · P2 · confianza alta**
**El progreso de serie en tarjetas divide el episodio DE LA TEMPORADA entre el total DE LA SERIE.**
`src/lib/library/progress.ts:18-29` calcula `position.episode / totalEpisodes`, pero
`rollSeriesProgress` guarda `position = {season, episode}` con numeración POR TEMPORADA
(`src/lib/series/episode-watch-store.ts:110-119`) y `totalEpisodes` es el total completo de la
serie (`get-library-items.ts:125`). Quien va por T3E2 de una serie de 60 episodios ve «2/60» (3 %)
en Inicio y en la tira Continuar, mientras el rail de la ficha calcula bien con `episode_watches`
(`serie/[id]/page.tsx:225-238`). Dos fórmulas para el mismo dato, una incorrecta.
*Impacto:* número falso en la pantalla más vista de la app.
*Recomendación:* derivar el progreso de tarjeta del conteo real de `episode_watches` (o guardar
el índice absoluto en `position`).

---

**F1-005 · funcional · BUG · P2 · confianza media-alta**
**Puntuar o desmarcar un episodio puede cerrar (o re-completar) el pase como efecto colateral.**
`src/lib/series/episode-actions.ts` (`setEpisodeWatched`, `rateEpisode` → `rollAndMaybeClose`):
`reachedEnd` solo compara «episodio más avanzado del pase» con «último del catálogo»
(`episode-watch-store.ts:135-141`). Desmarcar un episodio intermedio teniendo el final visto, o
puntuar el final, redispara `applyTransition(..., "completed")` — incluso sobre un pase `dropped`,
que pasa a `completed` con `finished_on` = hoy sin preguntar. En libro el auto-cierre exige una
sesión deliberada en la última página.
*Impacto:* gestos de edición cambian el estado del pase sin intención del usuario.
*Recomendación:* condicionar el auto-cierre a un delta positivo (marcar el último episodio) y
nunca resucitar un pase `dropped`.

---

**F1-006 · funcional · BUG · P2 · confianza media**
**`addSession` sobre un pase que la transición archiva escribe la sesión y el cursor en el pase
VIEJO.** `src/lib/sessions/actions.ts`: si la transición de `:247` resulta `archiveAndCreate`, la
sesión se inserta con el `pass_id` recibido (`:169-180`) y `position` se actualiza sobre él
(`:262-266`) — ambos acaban en el pase archivado, no en el nuevo. El auto-cierre de `:290` puede
además pisar un `dropped` recién elegido en la misma hoja.
*Impacto:* sesiones y progreso adjudicados al pase equivocado; historial incoherente.
*Recomendación:* usar el `passId` que devuelve `applyTransition` para todo lo posterior.

---

**F1-007 · cache-rendimiento · BUG · P2 · confianza alta**
**La importación CSV escribe notas sin invalidar `ratings:*` ni revalidar nada fuera de
`/importar`.** `src/lib/import/commit-row.ts:139-151` inserta pases `completed` con `rating`
(`is_public: true`); las actions de `src/app/importar/actions.ts` solo hacen
`revalidatePath("/importar/pendientes")` (`:258`, `:276`, `:361`). El comentario de
`revalidate.ts:101` («todo escritor de nota pasa por aquí») es falso para el import: la media
cacheada de `getRatingSummary` no cuenta las notas importadas hasta expirar `cacheLife("hours")`,
y ficha/perfil/feed no se revalidan.
*Impacto:* read-your-own-writes roto justo tras importar (la clase de bug que el repo señala como
la más cara: #36/#37/#39/#66).
*Recomendación:* al terminar un batch, invalidar `ratings:*` de los ítems tocados vía el módulo
central de reactividad.

### Riesgos

---

**F1-008 · datos · RIESGO · P1 · confianza alta**
**`credits` huérfanos masivos: 3.525 de 4.424 filas en dev (~80 %).**
Medido hoy: 1.380 huérfanos → movies, 633 → books, 1.512 → series. `credits` quedó fuera de
`private.forbid_delete_with_passes` y no hay ninguna otra guarda. La issue #609 conocía el
síntoma (un caso en movies); esto lo cuantifica en los tres tipos. Prod sin medir.
*Impacto:* fichas de persona que «mienten» mostrando filmografías vacías o rotas.
*Recomendación:* medir prod, limpiar huérfanos y decidir guarda (ver F1-009). Actualizar #609
con estas cifras.

---

**F1-009 · datos · RIESGO · P2 · confianza alta**
**Las referencias polimórficas `(item_type, item_id)` no tienen guarda de integridad salvo
`passes`.** El único cierre en BD es `private.forbid_delete_with_passes` (BEFORE DELETE en
books/movies/series, solo mira `passes`). Sin protección: `credits` (F1-008), `saga_items`
(1 huérfano ya en dev), `notes`, `collection_items`, `club_activity_items`, `posts.anchor_id`.
La integridad es «responsabilidad de la app», y F1-008 demuestra que no basta.
*Impacto:* filas huérfanas silenciosas en cualquier tabla polimórfica cuando se borra catálogo.
*Recomendación:* ampliar el trigger de borrado (o job periódico de detección de huérfanos).

---

**F1-010 · funcional · RIESGO · P2 · confianza media-alta**
**`applyTransition` degrada mal ante fallos.** `src/lib/passes/apply-transition.ts:44-93`:
(a) en `updateActive` traga el 23505 y devuelve `closed:true` → la hoja de cierre estampa
`finished_on` sobre un pase que sigue abierto; (b) `archiveAndCreate` archiva y luego inserta SIN
transacción → si el insert falla, la obra queda sin ningún pase `is_active` (invisible en
biblioteca; `promote_active_pass_after_delete` solo cubre DELETE) y devuelve como `passId` el del
archivado; (c) el comentario de `:49-50` cita el índice `one_pass_per_day`, borrado en
`20260717_pass_hub_c_rename.sql:354-357`.
*Impacto:* ventana estrecha pero real de estados corruptos en el hub central del modelo.
*Recomendación:* mover archivar+crear a un RPC transaccional; corregir el comentario.

---

**F1-011 · funcional · RIESGO · P2 · confianza alta**
**La máquina de estados de pases es solo convención de TypeScript.**
(a) `savePassFields` (`src/lib/passes/actions.ts`, verificado en `main`) filtra solo por
`id`+`user_id`: aplicada a un pase `planned`/`in_progress` deja un `in_progress` con
`finished_on` puesto; tampoco valida `finished_on >= started_on` (sin CHECK).
(b) `20260717_pass_hub_b4_hub_writes.sql:18-21` concede `insert/update (status, …)` a
`authenticated`: cualquier cliente REST puede saltarse `planTransition` sobre sus propias filas.
(c) El importador TS y `resolve_pending_import` ya la puentean hoy (F1-002).
*Impacto:* integridad propia (no fuga entre cuentas), pero convierte los invariantes de estado
en buenas intenciones.
*Recomendación:* CHECKs mínimos en BD (estado⟺fechas, orden de fechas); valorar canalizar la
escritura de `status` por RPC.

---

**F1-012 · funcional · RIESGO · P2 · confianza alta**
**Subida de portadas con service-role: el gate TS es la única barrera.**
`uploadCover` (`src/lib/catalog/edit-actions.ts:218-222`) y `uploadSagaCover`
(`curation-actions.ts:244-262`) escriben en Storage saltándose la RLS del bucket; el
`requireCollaborator` previo es la única defensa. Documentado en código pero frágil ante
refactors. (El resto del RBAC verificado está bien: doble barrera TS+BD en catálogo, sagas,
importación y admin, conforme a la decisión 8-H.)
*Impacto:* un refactor que reordene el gate abre escritura de Storage a cualquier usuario.
*Recomendación:* política de Storage que respalde el gate (o subida vía RPC con verificación).

---

**F1-013 · arquitectura · RIESGO · P2 · confianza alta**
**`loading.tsx` mata el 404 real de `/persona/[id]`** (regla del propio repo, escrita en
`src/app/estadisticas/loading.tsx:5-7`). La ruta tiene `loading.tsx` y su página llama
`notFound()` (`persona/[id]/page.tsx:66`): una persona inexistente devuelve HTTP 200 con 404
«soft» streameado. Mismo patrón heredado en `/coleccion/c/[id]` (el boundary de `/coleccion`
envuelve al hijo que llama `notFound()`, `c/[id]/page.tsx:56` — P3, ruta privada sin SEO).
Las fichas libro/serie/película lo hacen bien (not-found sin loading).
*Impacto:* SEO/crawlers indexan 200 vacíos; incoherencia con la regla propia. Emparenta con #514.
*Recomendación:* quitar el `loading.tsx` de persona (el Suspense interno ya cubre el skeleton).

---

**F1-014 · cache-rendimiento · RIESGO · P2 · confianza alta**
**`markAllNotificationsRead` hace `revalidatePath("/", "layout")` en cada apertura de la
campana.** `src/lib/social/notification-actions.ts:36`. Es la revalidación más cara que existe
(purga la Client Cache e invalida todo) pagada por la acción más frecuente de la app, para
actualizar un contador. Además puentea `src/lib/reactivity/revalidate.ts`.
*Impacto:* tira toda la caché de cliente en cada visita a notificaciones.
*Recomendación:* revalidación quirúrgica (tag propio del contador) vía el módulo central.

---

**F1-015 · cache-rendimiento · RIESGO · P2 · confianza media**
**Escritura durante render RSC: `markClubRead` (upsert) se ejecuta al renderizar
`ClubFeedSection`.** `src/app/club/[slug]/page.tsx:218` → `src/lib/clubs/unread.ts:36`.
Mutación en un GET; un render especulativo/prefetch marcaría el club como leído sin abrirlo.
*Impacto:* contadores de no-leídos poco fiables; anti-patrón RSC.
*Recomendación:* mover a `after()` (patrón que las fichas ya usan para hidratación).

---

**F1-016 · arquitectura · RIESGO · P2 · confianza media**
**`.in()` sin trocear con listas del tamaño de la biblioteca entera.**
`src/lib/supabase/in-chunks.ts` existe exactamente para esto (~11 KB de query string con 300
UUIDs, límite típico 8 KB), pero solo lo usa `people/get-person-profile.ts`.
`library/get-library-items.ts:141,168` mete todas las claves de la biblioteca del usuario en dos
`.in("item_id", …)` sin chunk; igual los fan-outs de `sagas/get-saga-detail.ts` y `social/feed.ts`.
*Impacto:* fallo silencioso (página vacía sin error) al crecer la biblioteca (~300+ obras).
*Recomendación:* pasar los fan-outs polimórficos por `inChunks` (encaja con F1-020).

---

**F1-017 · datos · RIESGO · P2 · confianza media-alta**
**Migraciones aplicadas a dev sin fichero en ninguna rama, una con nombre que miente.**
El ledger de dev tiene `20260861_fix_profiles_insert_role_escalation` y
`20260862_fix_pass_reviews_security_invoker`; no existe fichero en `git log --all`. La primera
está viva (policy `profiles insert own` exige `role = 'user'`). La segunda es incoherente:
`pass_reviews` NO tiene `security_invoker` (reloptions null, owner `postgres`) y el advisor la
marca ERROR `security_definer_view`. Puede ser deliberado (la vista ES la que aplica la
privacidad de `review`), pero entonces el nombre y el rastro de esa migración desinforman.
*Impacto:* imposible reconstruir el esquema desde el repo; próxima persona en seguridad de
`pass_reviews` hereda una pista falsa. Toca de lleno a #690/#688 (P0 abiertas).
*Recomendación:* recuperar/recrear los ficheros y aclarar en issue qué hizo de verdad `20260862`.

---

**F1-018 · datos · RIESGO · P2 · confianza alta**
**La migración F del PR #674 (`20260818_catalog_f_revoke_insert.sql`, sin commitear) puede
quedar huérfana.** Dev tiene aplicadas `catalog_a…e` pero NO la F: las policies
`catalog * insertable` siguen vivas e `information_schema.table_privileges` confirma INSERT
concedido a `anon` Y `authenticated` en books/movies/series. El fichero manda aplicarla solo con
el código nuevo desplegado (correcto), pero nada la rastrea si el PR se mergea sin ella.
*Impacto:* la ventana de INSERT directo al catálogo global (el problema que #674 cierra) queda
abierta indefinidamente sin que nadie lo note.
*Recomendación:* issue de seguimiento ligada al despliegue de #674.

---

**F1-019 · funcional · RIESGO · P3 · confianza media**
**Reimportación concurrente duplica pases históricos.** La idempotencia de `/importar` es solo
de aplicación: `commit-row.ts:129-137` comprueba «¿ya hay pase con este `finished_on`?» sin
índice único que lo respalde (su comentario `:97-101` lo admite). Dos importaciones simultáneas
del mismo CSV duplican historial (infla diario y estadísticas).
*Impacto:* bajo en probabilidad, molesto de limpiar cuando pasa.
*Recomendación:* índice único parcial (user, item, finished_on) o candado por batch.

### Deuda técnica

---

**F1-020 · arquitectura · DEUDA · P2 · confianza alta**
**La hidratación polimórfica de catálogo está re-implementada ≥10 veces (la abstracción ausente
nº 1 del proyecto).** El patrón «agrupar ids por tipo → 3 queries books/movies/series → mapa
`tipo:id → {title ?? UNTITLED_FALLBACK, cover}`» está copiado casi literal en:
`clubs/activities/core.ts:365-380` y `:427-439` (dos veces en el mismo fichero),
`clubs/activities/event-detail.ts:196-215`, `notes/get-notes.ts:77-93`,
`pace/fetch-catalog-meta.ts:51-111`, `social/feed.ts:248-302`, `social/recent-reviews.ts:116-146`,
`social/anchor-search.ts:40-106`, `library/get-library-items.ts:48-127`,
`people/get-person.ts:88-96`, `stats/get-rated-facets.ts:79-96`, más variantes en `sagas/`.
Existe helper parcial — `stats/get-item-titles.ts` — que solo usa stats. Satélites del mismo eje:
`groupIdsByType` (`catalog/group-ids-by-type.ts`) con 13 re-implementaciones inline del bucket
`{book:[],movie:[],series:[]}`, y la resolución «tipo → tabla» codificada 3 veces
(`import/commit-row.ts:16`, `notes/get-notes.ts:244`, `stats/get-item-titles.ts:21-25`).
*Impacto:* cada cambio de contrato del catálogo (p. ej. el title nullable de las shells, #674)
obliga a tocar ~10 sitios; `UNTITLED_FALLBACK` ya tuvo que sembrarse copia a copia.
*Recomendación:* un único hidratador en `lib/catalog` (con `inChunks` dentro — resuelve también
F1-016) al que migren los llamadores gradualmente.

---

**F1-021 · arquitectura · DEUDA · P2 · confianza alta**
**Las tres fichas de obra triplican el mismo esqueleto (~570 líneas cada una).**
`libro/[id]/page.tsx` (573), `serie/[id]/page.tsx` (572), `pelicula/[id]/page.tsx` (526):
mismo `generateMetadata` → `XDetailPage` → `XDetail` → `XTabs` con el mismo `Promise.all`
comentado. Un cambio transversal (como #674) se aplica tres veces, y las divergencias no
intencionadas ya existen (F1-001, F1-024, F1-027).
*Impacto:* la fuente principal de asimetrías entre libros/películas/series.
*Recomendación:* extraer el esqueleto común por partes (metadata, panel de comunidad, tabs),
no un mega-refactor de golpe.

---

**F1-022 · arquitectura · DEUDA · P2 · confianza alta**
**Cinco componentes gigantes concentran el riesgo de regresión.**
`detail/catalog-editor.tsx` (942 líneas, un solo form de ~600), `stats/panel/charts.tsx` (854),
`detail/log-panel.tsx` (758, mezcla ManagedLog + PassDataPanel + varios paneles),
`clubs/propose/event-form.tsx` (651), `clubs/activity-detail.tsx` (541).
*Impacto:* coste de revisión alto en rutas calientes (catalog-editor y log-panel).
*Recomendación:* trocear cuando se toquen; no urgente por sí solo.

---

**F1-023 · cache-rendimiento · DEUDA · P2 · confianza media**
**Los tags `credits:*` y `saga-membership:*` se declaran pero nadie los invalida jamás**
(cero `updateTag`/`revalidateTag` fuera de `ratings:*`). Consecuencias: (a) una visita anónima a
una obra sin enriquecer cachea créditos VACÍOS (`ensureItemEnriched` no escribe sin sesión,
`pelicula/[id]/page.tsx:285`) y se sirven hasta agotar `cacheLife("days")` aunque alguien
enriquezca después; (b) añadir un ítem a una saga cambia el «nº X de Y» de TODOS los miembros,
pero `addItemToSaga` solo revalida la ficha del ítem tocado (`manage-saga-actions.ts:112`,
`curation-actions.ts:161`).
*Impacto:* datos rancios días; mitigado por la ventana y el refresco-al-navegar temporal de Next.
*Recomendación:* invalidar los tags en los escritores correspondientes.

---

**F1-024 · funcional · DEUDA · P2 · confianza alta**
**Desde un libro no se puede llegar a la ficha de su autor.**
`libro/[id]/page.tsx:333` calcula `credits` y no monta `CreditsSection` (película `:431-435` y
serie `:525` sí); el autor es texto plano (`libro:433`). `/persona/[id]` solo es alcanzable desde
reparto de cine/series. Familia de #621 (que cubre Dirección/Creación) pero deja fuera a TODO el
dominio libro.
*Impacto:* feature completa (ficha de persona) invisible para el dominio libro.
*Recomendación:* montar `CreditsSection` en libro (o registrar acta de por qué no).

---

**F1-025 · funcional · DEUDA · P2 · confianza alta**
**Media app construida con descubribilidad casi nula.**
- `/cuenta/contrasena`: **huérfana** — 0 enlaces en `src/`; solo se llega por el mail de
  recuperación (`(auth)/actions.ts:160`). Un usuario logueado no puede cambiar su contraseña
  desde la app.
- `/importar` (+`/pendientes`): solo desde el panel «editar perfil» (`edit-profile-form.tsx:71`).
- `/notas` y `/estadisticas`: cuelgan de enlaces de segundo nivel en pestañas owner-only del
  perfil (`memorize-card.tsx:75`, `stats-tab.tsx:158`).
- `/sagas`: fuera de la nav; un enlace discreto en Buscar (`buscar/page.tsx:92`) y un empty
  state que desaparece al seguir 1 saga (`coleccion/page.tsx:377`).
26 de 41 rutas cuelgan solo de enlaces contextuales.
*Impacto:* trabajo hecho que los usuarios no encuentran; la contraseña es además un hueco de UX
básico.
*Recomendación:* pasada de navegación deliberada (ajustes con cambio de contraseña e importar;
entradas visibles para notas/estadísticas/sagas).

---

**F1-026 · funcional · DEUDA · P2 · confianza alta**
**Paridad de serie en «Mi registro»: hueco preparado y sin cablear.** La pestaña Registro de una
serie no dice por dónde vas (`log-panel.tsx:299-312` calcula progreso solo para `book`;
`serie/page.tsx` no pasa `workTotalUnits` aunque la firma lo admite) y no permite anclar una nota
a un episodio (`log-panel.tsx:427` da `kind:"none"` a la serie) pese a que compositor, `addNote`
y el orden de `/notas` YA soportan el ancla temporada+episodio. La película tiene decisión
registrada (2026-07-30); la serie no.
*Impacto:* asimetría no decidida; backend listo desaprovechado.
*Recomendación:* cablear las props o registrar la decisión de no hacerlo.

---

**F1-027 · cache-rendimiento · DEUDA · P2 · confianza alta**
**`auth.getUser()` de red en lectores RSC, contra la regla del repo (reincidencia del #283).**
`getCurrentUserRole` (`src/lib/auth/roles.ts:49-51`) llama a `supabase.auth.getUser()` (viaje de
red a `/auth/v1/user`) en vez de `getCurrentUser()` memoizado; la ficha lo llama 2 veces por
render (`pelicula/[id]/page.tsx:176`, `:306`). Mismo patrón en `getClub` (`clubs/clubs.ts:119-122`),
`getSagaDetail` (`get-saga-detail.ts:341-343`), `getClubUnreadCounts` (`unread.ts:13-15`),
`listMyClubs` (`clubs.ts:186-188`), `getInteractionSummary` (`interactions.ts:140`).
Y en `/club/[slug]`, `getClub` entero (4 viajes) se ejecuta DOS veces por request — lo llaman
`generateMetadata` (`page.tsx:51`) y el cuerpo (`:68`) sin `cache()` de React.
*Impacto:* ~240 ms/viaje redundante antes del primer byte en rutas calientes.
*Recomendación:* `getCurrentUser()` en todos; envolver `getClub` en `cache()`.

---

**F1-028 · arquitectura · DEUDA · P3 · confianza alta**
**Server actions en tres ubicaciones y dos convenciones; `lib/clubs` marca módulos de dominio
enteros como `"use server"`.** Actions en `src/app/*/actions.ts` (7), `src/lib/**/*-actions.ts`
(55) y `src/components/clubs/` (2). `lib/clubs` usa nombres sin sufijo (`clubs.ts`, `posts.ts`,
`membership.ts`, `rounds/*.ts`…) mezclando lecturas y mutaciones bajo `"use server"`: cada export
es un endpoint POST invocable aunque se pensara como helper (mitigado por RLS). No hay sitio
único donde auditar mutaciones.
*Impacto:* superficie de red mayor de la intencionada; fricción de auditoría.
*Recomendación:* convención única (`*-actions.ts` solo mutaciones); separar lecturas de clubs.

---

**F1-029 · arquitectura · DEUDA · P3 · confianza alta**
**Duplicaciones menores de lógica de dominio** (mismo eje que F1-020 pero de menor daño):
- Media de notas implementada 6+ veces con divergencia de redondeo: helper `averageRating`
  (`series/rating-scale.ts:27-30`) re-implementado en `community/get-community.ts:196-197`,
  `series/get-episode-data.ts:93-97` y `:167` (dos veces en el módulo que ya tiene el helper),
  `sagas/build-library-saga-cards.ts:295`, y sin redondear en `library/collections.ts:216` y
  `people/get-person-profile.ts:263,319-320`.
- «Último pase cerrado»: `community/latest-rating.ts:17-32` desempata por `id` a propósito;
  `sagas/build-library-saga-cards.ts:126-131` re-implementa el criterio sin desempate.
- Doble fetch del pase activo en biblioteca: `getLibraryItems` consulta los pases y
  `hydrateItems` vuelve a pedir las mismas filas (`get-library-items.ts:286-301` + `:136-141`,
  justificado en comentario pero redundante para la ruta principal).
- Pase activo consultado 2× por ficha: hero (`pelicula/[id]/page.tsx:166-175`) y tabs (`:296-304`)
  repiten la misma query en vez de pasarla por props; idéntico en libro y serie.
*Impacto:* bajo por pieza; erosión de «una definición por concepto».
*Recomendación:* consolidar al tocar cada zona; no urgente.

---

**F1-030 · cache-rendimiento · DEUDA · P3 · confianza alta**
**Bypasses del módulo central de revalidación** (`revalidate.ts:6-8` exige pasar por los
helpers): `library/quick-add-actions.ts:30,67` crean pase y solo revalidan `"/"` (ni colección ni
ficha; hoy lo tapa el refresco-al-navegar temporal de Next, que desaparecerá);
`collection-actions.ts:31,44,68,98,146,192` usan `revalidatePath("/coleccion")` a pelo teniendo
`revalidateLibrary()`; `notify-actions.ts:43` ídem con `revalidateProfile()`.
*Impacto:* UI rancia latente cuando Next retire el refresco temporal.
*Recomendación:* pasar todos por los helpers; añadir helpers para admin/importar/buscar.

---

**F1-031 · datos · DEUDA · P3 · confianza alta**
**Higiene de BD menor.** (a) 11 RPCs SECURITY DEFINER recientes sin `pg_temp` en `search_path`
(`register_catalog_item(s_bulk)`, `hydrate_*_bulk` — las del propio #674 —,
`get_activities_progress`, `update_activity_details`, `archive_club_activity`,
`pull_pending_celebrations`, `enforce_catalog_edit_collaborator_only`): desviación de la
plantilla que la tanda `20260808` corrigió en 55 funciones; no explotable hoy.
(b) Ledger de dev sucio: `activities_progress` aplicada 3 veces bajo dos nombres, colisión
`20260853`, pares aplicar+revertir sin fichero — nada roto, pero confirma la regla «el ledger no
es fuente de verdad». (c) 23 WARN `anon_security_definer_function_executable` en advisors
merecen un barrido puntual (el doc de la tanda 2026-07-29 presumía «cero ACL con anon»).
(d) Índices de `passes` suficientes hoy (308 filas); anotar «histórico por user+item cerrados»
para cuando crezca.
*Recomendación:* barrido único de search_path + advisors cuando se toque BD; sin urgencia.

---

**F1-032 · arquitectura · DEUDA · P3 · confianza alta**
**Menudencias de arquitectura/UI.**
- Dos familias de tabs conviviendo (Server-Link con URL: `ClubTabs`/`CollectionTabs`/`SectionTabs`
  vs cliente con slots + `?tab=`: `ItemDetailTabs`/`SagaTabs`); `ClubTabs` mezcla `?tab=` con la
  subruta real `/calendario` (`club-tabs.tsx:12-14`, autodocumentado como parche).
- `viewerLoggedIn` drilled por 31 ficheros; en `social/milestone-card.tsx:24` es prop muerta.
- Tres módulos de imagen con frontera difusa: `lib/image/` (webp), `lib/images/` (cdn-loader,
  que además parece huérfano pero es el `images.loaderFile` de `next.config.ts` — merece
  comentario defensivo) y `lib/storage/` (upload).
- `lib/auth/roles.ts` sin `"server-only"`.
- CTA del rail de película «Registrar visionado» lleva a `?tab=log`, donde ese botón no existe
  (`pelicula:239-240`).
- Dos porcentajes distintos para el MISMO libro en la misma página: rail contra
  `books.total_pages` (`libro:213-221`) vs Registro contra páginas de la edición del pase
  (`log-panel.tsx:309-312`).
- `SagaStrip` exige ≥2 miembros en serie y ≥1 en libro/película (`serie:492` vs `libro:491`).
*Recomendación:* arreglos de una línea al pasar; elegir familia de tabs para dominios nuevos.

### Legacy y obsoleto

---

**F1-033 · obsoleto · LEGACY · P3 · confianza alta**
**Seis ficheros completamente muertos, con historia trazada.**
| Fichero | Origen de la muerte | Riesgo de borrado |
|---|---|---|
| `src/lib/diary/` (carpeta: `get-monthly-activity.ts`, `types.ts`) | último resto del modelo `diary_entries`; perdió su llamador en `3ae384e8` (2026-08-04, #423) | bajo |
| `src/components/library/continue-strip.tsx` | salió de Colección v2 (`db39867a`, 2026-07-18); solo lo cita un comentario | bajo |
| `src/components/now-consuming.tsx` | sustituido por «¿Qué has disfrutado hoy?» (`62622963`) | bajo |
| `src/lib/sagas/apply-membership-ops.ts` + `editor-types.ts` (+ test) | cadena muerta del editor de grafo retirado (2026-07-26) | bajo-medio |
| `src/lib/sagas/linearize-graph.ts` (+ test) | absorbe­do por itinerarios (`5f1f0d44`); su cabecera aún cita `saga_nodes/saga_edges` (tablas dropeadas) | bajo |
| `src/lib/social/recent-reviews.ts` | «previews legadas» según `feed.ts:55,109`; sin llamador de producción; **tocada ayer en #674 estando muerta** | medio — confirmar contra `shared-activity.ts` y reescribir su parte de `sort-date-contract.test.ts` |

Verificaciones hechas por candidato: grep de path de import (cubre `@/`, relativos y dinámicos)
en src/e2e/scripts/public/configs, grep por símbolo repo-wide, referencias por string,
`git log -S`. *Recomendación:* borrar en fase de limpieza con esa verificación re-ejecutada.

---

**F1-034 · obsoleto · LEGACY · P3 · confianza media**
**~14 exports muertos en ficheros vivos; dos huelen a feature a medio cablear.**
Riesgo medio (comprobar intención antes de borrar): `getFollowedEvents`/`getFollowedEventIds`
(`clubs/activities/event-detail.ts:360,420` — implementan §16/§17 de la spec de seguimiento de
eventos 2026-08-04, sin llamador ni test) y `groupPersonEntries`/`chunkIntoCards`/
`descriptorForEntry`/`descriptorForEvent` (`social/group-feed-entries.ts:36-129`, agrupador del
feed pre-posts; el tipo `PersonGroupEntry` del mismo fichero SÍ está vivo).
Riesgo bajo: `getPerson` (`people/get-person.ts:122`), `EVENT_TYPES` (`event-types.ts:8`),
`onCelebrationPreferenceChange` (`celebrations/preference.ts:24`), `isFcmConfigured`
(`push/fcm.ts:41`), `itemFilterNote` (`stats/filter.ts:50`), `SHELL_FORM` (`ui/layout.ts:16`,
docstring desfasado), `ClubListSkeleton`, 4 iconos de `ui/icons.tsx`, `ReactableTargetType`,
`ParsedImport`. Ningún hook muerto; ningún flag muerto; **cero dependencias de package.json sin
uso** (verificadas las 31).
*Recomendación:* para los dos de riesgo medio, decidir cablear o abrir issue `tipo:acta`;
el resto, borrado trivial en fase de limpieza.

---

**F1-035 · obsoleto · LEGACY · P3 · confianza alta**
**Prosa y esquema que aún hablan del mundo pre-`passes`** (el código está limpio: cero lecturas
de `library_entries`/`diary_entries` en `src/` — verificado con grep; solo F1-003 escribe desde
BD). Comentarios TS: `library/position.ts:19-20` («library_entries.position…»),
`detail/log-panel.tsx:534-538` (ventana transicional Tarea 7→9 ya cerrada),
`detail/community-panel.tsx:10`, `library/types.ts`. En BD: `validate_club_post_ref` aún acepta
`sourceTable` `'library_entries'`/`'diary_entries'`; `COMMENT ON TABLE` fósiles en `notes`
(«como challenges/queues» — queues no existe), `reactions` («hoy solo like» — paleta ampliada en
`20260837`), `progress_sessions` y `challenges` (citan diary_entries).
*Impacto:* en un proyecto que usa comentarios como memoria de agentes, cada uno es el cebo del
próximo PR #96.
*Recomendación:* barrido de comentarios + migración de `COMMENT ON` en la fase de limpieza.

---

**F1-036 · doc · DEUDA · P2 · confianza alta**
**El mapa de arquitectura derivado lleva 812 commits de retraso.**
`docs/architecture/graph.json` se generó el 2026-07-29 (`meta.generatedAt`); desde entonces hay
812 commits (medido con `git log --since`). Sigue siendo útil (esta auditoría lo usó de índice)
pero sus `flows[].steps[].file` —lo que lo hace valioso— son justo lo primero que caduca.
Satélites: `data-model.md` §8 con números viejos (dice 69 funciones/62 con pg_temp; hoy 125/61),
y `database.types.ts` desincronizado ya en issue #654 (en esta rama falta el enum
`saga_placement = 'anclado'` que dev ya tiene — regenerar al rebasar).
*Recomendación:* regenerar graph.json y refrescar §8 tras cerrar #674.

---

**F1-037 · obsoleto · LEGACY · P3 · confianza alta**
**Inventario TODO/FIXME: excepcionalmente limpio.** No existe ningún `FIXME`/`HACK`/`XXX`/
`@deprecated` en el repo. Hay UN único TODO real, repetido 59 veces con texto idéntico
(«Cache Components adoption. Refactor this route so this opt-out can be removed.») en las 59
rutas — es la checklist viva de la migración #448, no deuda olvidada. Scripts one-shot
(`scripts/backfill-genres.ts`, `backfill-sizes.ts`): cumplieron su función, valor documental,
retirar solo tras verificar prod. Los `e2e/*-v2*.spec.ts` NO son versiones viejas (v2 es el
nombre de la feature). Los backups JSON de prod trackeados en git (`backups/`) ya están cubiertos
por la issue **#677 (P0)** — no se duplica aquí.

### C. Contexto: issues P0/P1 abiertas relevantes (ya detectadas, no re-halladas)

**P0:** #690/#688 (bypass RLS `pass_reviews`: escritura de reseñas ajenas — F1-017 aporta la
pista de la migración fantasma), #689/#687 (escalada user→admin pre-onboarding), #677 (backup
real de prod en git), #674 (envenenamiento del catálogo global — en curso en la rama auditada).
**P1:** #699 (`hydrate_book` falla para rol `user`), #691 (default privileges ALL a
anon/authenticated), #678 (SSRF vía Web Push), #676 (DoS `total_seasons`), #675 (bypass RPCs
sagas TMDB — re-verificado: el alta arbitraria sigue viva), #654 (`database.types.ts`
desincronizado), #643 (nota media de saga en base 10), #609 (créditos huérfanos — F1-008 lo
cuantifica), #584 (e2e tumba dev server), #582 (doc hora de Encuentro), #514 (PPR devuelve 200
donde dev da 404 — emparenta con F1-013).
**Ampliación de issue existente:** el alcance de #628 (hitos sin publicar) incluye también
`series/episode-actions.ts:45,63`, que la issue no menciona.

---

## 1. Resumen del estado arquitectónico

El proyecto está **mejor de lo que su tamaño y velocidad harían temer**. Los tres invariantes
duros se cumplen en el código de aplicación con disciplina verificada, no supuesta:

- **passes-hub:** cero lecturas de `library_entries`/`diary_entries` en `src/` (grep exhaustivo);
  todo estado de usuario deriva de `passes`. La única violación viva está en BD (F1-003), no en
  la app.
- **Caché/RLS (regla #437):** solo 3 `use cache` en todo el repo y los tres correctos
  (`createPublicClient` + argumentos escalares + tag). **Cero fugas entre cuentas.** Las
  funciones sensibles llevan comentarios que citan la regla para NO cachearlas.
- **Fuentes de verdad únicas respetadas:** transiciones (`planTransition`), «completado» de
  sagas (`completion.ts`), rol (`auth/roles.ts`), nota↔dots, `itemHref`, `UNTITLED_FALLBACK`.
- **Capa app sana:** ninguna `page.tsx` cliente; islas cliente que derivan de props y
  reconcilian con `router.refresh()`; todos los mutadores comprueban auth; 0 route handlers
  huérfanos; 0 RPCs rotas u huérfanas; 0 dependencias sobrantes.

Las grietas reales son de otra naturaleza: (1) **los invariantes viven solo en TypeScript** —
la BD no los respalda (F1-011: grants de `status`, sin CHECKs estado⟺fechas; F1-009: integridad
polimórfica sin guarda salvo `passes`) y cada vía paralela (importador, RPCs, triggers) los
puentea; (2) **la abstracción ausente de hidratación polimórfica** multiplica por 10 el coste de
cada cambio de contrato del catálogo (F1-020); (3) **la triplicación de las fichas** genera
asimetrías no decididas entre tipos, con la serie como pariente pobre sistemático (F1-001,
F1-004, F1-026); (4) el **modelo Obra→Ejemplar→Pase se respeta**, pero sus efectos colaterales
de cierre automático (episodios, sesiones) toman decisiones por el usuario (F1-005, F1-006).

## 2. Principales problemas funcionales

1. Reseñas de serie invisibles para siempre (F1-001).
2. Pases importados sin fechas que la app trata como lecturas en curso (F1-002) + el importador
   como fábrica general de estados que puentean la máquina (F1-011, F1-019).
3. Progreso de serie falso en las tarjetas de Inicio/Continuar (F1-004).
4. Gestos sobre episodios/sesiones que cierran o resucitan pases sin querer (F1-005, F1-006).
5. «Auto-añadir a biblioteca» desde clubes escribe en la tabla muerta y no hace nada (F1-003).
6. Media app inaccesible: cambio de contraseña huérfano, importar/notas/estadísticas/sagas
   enterradas (F1-025), persona inalcanzable desde libros (F1-024).

## 3. Principales deudas técnicas

1. Hidratador polimórfico ausente — 10+ copias del mismo patrón (F1-020) + `.in()` sin trocear
   (F1-016). Un solo refactor resuelve ambos.
2. Fichas de obra triplicadas (F1-021) y 5 componentes gigantes (F1-022).
3. Invariantes de `passes` sin respaldo en BD (F1-011) e integridad polimórfica sin guarda
   (F1-009).
4. Revalidación: tags declarados que nadie invalida (F1-023), import sin invalidación (F1-007),
   `revalidatePath("/", "layout")` en la campana (F1-014), bypasses del módulo central (F1-030).
5. `auth.getUser()` de red reincidente en RSC + `getClub` doble (F1-027).
6. Migraciones fantasma y ledger sucio (F1-017, F1-031b) — la reconstruibilidad del esquema
   desde el repo está comprometida.

## 4. Elementos potencialmente legacy a investigar en fases posteriores

1. Los 6 ficheros muertos de F1-033 (re-verificar y borrar; `recent-reviews.ts` exige confirmar
   contra `shared-activity.ts` y ajustar `sort-date-contract.test.ts`).
2. `getFollowedEvents`/`getFollowedEventIds`: ¿feature de seguimiento de eventos a medio cablear
   o abandonada? Decidir y registrar (F1-034).
3. La migración fantasma `20260862_fix_pass_reviews_security_invoker`: qué hizo de verdad y si
   el estado actual de `pass_reviews` es el deseado (F1-017; toca a #690/#688).
4. Prosa pre-`passes` en comentarios TS y `COMMENT ON TABLE` (F1-035).
5. Triggers `autoadd_library_on_activity_*`: ¿resucitarlos hacia `passes` o acta de retirada?
   (F1-003). Verificar prod.
6. Scripts backfill one-shot y exports muertos de riesgo bajo (F1-034, F1-037).

## 5. Top 10 acciones de mayor impacto detectadas en esta fase

| # | Acción | Hallazgos | Coste estimado |
|---|---|---|---|
| 1 | Mostrar las reseñas de pase de serie (arreglo de condición en `community-panel`) | F1-001 | bajo |
| 2 | Normalizar fechas/estado en el importador (TS + `resolve_pending_import`) y añadir CHECK estado⟺fechas en `passes` | F1-002, F1-011 | medio |
| 3 | Corregir la fórmula de progreso de serie en tarjetas | F1-004 | bajo |
| 4 | Domar los auto-cierres: episodios (delta positivo, nunca resucitar `dropped`) y sesiones (usar el `passId` devuelto) | F1-005, F1-006 | medio |
| 5 | Resolver los triggers de `library_entries` (crear pase o retirar + acta) y verificar prod | F1-003 | bajo |
| 6 | Limpiar `credits` huérfanos (dev y prod) y ampliar la guarda de borrado polimórfico | F1-008, F1-009 | medio |
| 7 | Extraer el hidratador polimórfico único con `inChunks` dentro; migrar llamadores gradualmente | F1-020, F1-016, F1-029 | alto (troceable) |
| 8 | Pasada de revalidación: invalidar `ratings:*` en import, tags `credits:*`/`saga-membership:*`, quitar el `revalidatePath("/", "layout")` de la campana, cerrar bypasses | F1-007, F1-014, F1-023, F1-030 | medio |
| 9 | Pasada de navegación/descubribilidad: contraseña, importar, notas, estadísticas, sagas; `CreditsSection` en libro | F1-025, F1-024 | medio |
| 10 | Higiene: borrar los 6 ficheros muertos + exports de riesgo bajo, quitar `loading.tsx` de persona, `getCurrentUser()` en los 6 lectores RSC, `cache()` en `getClub` | F1-033, F1-034, F1-013, F1-027 | bajo |

> Regla del repo: todo lo accionable de esta lista debe convertirse en issues etiquetadas
> (área/tipo/prioridad) antes de ejecutarse — se hará al arrancar la fase 2, para poder discutir
> primero este informe completo sin abrir 30 issues de golpe.

---

## Fase 3 — UI/UX Desktop

> Método: recorrido REAL de la app (`next dev` local, rama `fix/674-catalogo-server-authoritative`,
> BD dev con datos e2e, sesión `@devtest` con rol admin) en Chrome a **1920×1080** (viewport CSS
> ~1568px). Rutas visitadas: Inicio, Colección (Todo/Colecciones/Sagas), perfil público (3
> pestañas), ficha libro (The Hobbit, 3 pestañas), ficha película (Matrix), ficha serie (The Boys,
> 4 pestañas), saga (Matrix - Colección), índice /sagas, mapa de universo, feed (=Inicio), /clubes,
> club (Feed/Actividades/Calendario), /buscar (3 tipos), /estadisticas, /importar, /notas,
> /cuenta/contrasena, /admin, /persona, /genero, editor de catálogo inline, modal Editar perfil,
> not-found de ficha. **Solo diagnóstico; sin cambios de código.** NO auditado: onboarding (exige
> cuenta nueva), móvil/tablet, tema oscuro, /post/[id], /sesion.
>
> Sesgo de datos conocido: la BD dev tiene títulos e2e sin portada; donde eso empaña el juicio
> visual se señala. Convención de IDs: `F3-###`, mismas dimensiones que la fase 1.

### Diagnóstico en una frase

La app tiene una identidad visual sólida (serif + paleta cálida + portadas) y CERO diseño de
página desktop: casi todas las vistas son **una columna de móvil ensanchada**, y las decisiones
de componente (tabs, cards, botones, acciones destructivas, estados vacíos) se han tomado
vista a vista, sin sistema. No hay 40 problemas: hay ~8 patrones sin definir.

### S1 · Ancho y contenedores: el desktop es un móvil ancho

---

**F3-001 · layout · P1**
**Vista:** fichas de libro / película / serie (la plantilla más importante de la app).
**Problema:** bajo la tarjeta de metadatos (~250px de alto), TODO el tercio derecho de la página
queda vacío el resto del scroll; el contenido real (sinopsis, ediciones, dónde verla) vive en una
franja de ~620px. La ficha usa ~40 % del viewport útil.
**Evidencia:** capturas de The Hobbit y Matrix: sinopsis+ediciones en columna estrecha izquierda,
vacío a la derecha desde y≈500. En serie, ídem.
**Impacto:** la pantalla que define el producto se percibe pobre en desktop; obliga a scroll que
en 1920px no haría falta.
**Patrón relacionado:** S2 (mismo esqueleto triplicado, F1-021: arreglarlo son 3 sitios hoy).
**Propuesta:** ficha a 2 columnas reales de arriba abajo: columna principal (sinopsis, ediciones,
dónde verla, comunidad) + sidebar persistente (metadatos, saga, acciones de colección, resumen de
valoraciones). La pestaña Comunidad de libro hoy contiene 1 reseña + 1 tarjeta de media: cabe en
la columna/side de Información sin pestaña aparte.
**Complejidad:** media (por ficha; ×3 por F1-021). **Prioridad:** P1.

---

**F3-002 · layout · P2**
**Vista:** Inicio.
**Problema:** el feed —columna central y corazón social— mide ~415px a 1920 (≈27 % del viewport);
la columna personal izquierda muere tras «Para más tarde» dejando un vacío vertical enorme, y las
tarjetas de club del feed son solo texto (ni portada ni avatar real), así que el centro de la
página pierde la identidad visual de portadas que sí tienen los rails.
**Evidencia:** capturas de Inicio: 3 columnas 390/415/240 con gutters ~250px a cada lado; tarjetas
«Club · Test Public Club» sin imagen.
**Impacto:** la home entera se lee como tres tiras flotando en un mar beige.
**Patrón relacionado:** S5 (cards), F1-032.
**Propuesta:** dar al feed el ancho sobrante (600–680px), anclar portada/avatar en TODA entrada
de feed, y rellenar la columna personal con algo persistente (actividad de sagas, objetivo del
día) o dejarla no-sticky.
**Complejidad:** media. **Prioridad:** P2.

---

**F3-003 · layout · P2**
**Vista:** Buscar y Notas (Cuaderno).
**Problema:** patrón repetido de «fila de cabecera estirada»: input de búsqueda de ~190–290px
pegado a la izquierda y botón «Buscar» a ~1000px de distancia en el borde derecho. Control y
acción no forman un grupo; el input además es minúsculo para ser el protagonista de la vista.
**Evidencia:** capturas de /buscar (input 190px, botón en x≈1330) y /notas (ídem).
**Impacto:** la vista de búsqueda —puerta de entrada al catálogo— parece rota a primera vista.
**Patrón relacionado:** S6 (composición de formularios).
**Propuesta:** input grande (min 480px) con el botón adosado (o submit-on-enter y sin botón);
misma corrección en ambas vistas porque es el mismo componente conceptual.
**Complejidad:** baja. **Prioridad:** P2.

---

**F3-004 · layout · P3**
**Vista:** /importar, /cuenta/contrasena, /admin, saga.
**Problema:** las vistas «utilitarias» no tienen contenedor ni identidad: /importar es un input
de fichero nativo flotando a media pantalla; /cuenta/contrasena es un form suelto sin migas ni
contexto (y sin campo de confirmación); /admin es una tabla de ~550px con `<select>` nativos sin
estilar en una página vacía; la ficha de saga es un héroe centrado + una columna de ~770px.
**Evidencia:** capturas de las cuatro rutas.
**Impacto:** medio-bajo por separado; en conjunto, la mitad «herramienta» de la app parece de
otro producto.
**Patrón relacionado:** F1-025 (estas rutas además son casi inalcanzables).
**Propuesta:** contenedor estándar de página utilitaria (título + descripción + card de
contenido, max-width ~720px centrado) y sustituir los controles nativos por los del sistema.
**Complejidad:** baja por vista. **Prioridad:** P3 (P2 si se hace junto a la pasada de F1-025).

### S2 · Las tres fichas: mismos conceptos, tres diseños

---

**F3-005 · jerarquía · P1**
**Vista:** fichas libro/película/serie, pestaña Información.
**Problema:** el ORDEN de las secciones es distinto por tipo: libro = Sinopsis → Ediciones;
película = Reparto → Sagas → Sinopsis → Dónde verla → Versiones; serie = Sinopsis → Reparto →
Dónde verla. Y el nº/orden de pestañas también: libro 3 (Información/Comunidad/Mi registro),
película 2, serie 4 (con Episodios). El mismo concepto («la ficha de una obra») se aprende tres
veces.
**Evidencia:** capturas de las tres fichas.
**Impacto:** cada vertical parece una app distinta — exactamente lo que la auditoría debía
detectar; además es la manifestación visual de F1-021 (código triplicado ⇒ decisiones
triplicadas).
**Patrón relacionado:** F1-021, F1-024, F1-026.
**Propuesta:** definir UNA plantilla canónica de ficha (orden fijo: identificación → sinopsis →
lo específico del tipo → procedencia/dónde → comunidad) y que cada tipo solo INSERTE sus
secciones propias (ediciones / dónde verla+versiones / episodios) en el hueco reservado.
**Complejidad:** media-alta (es el refactor F1-021 con criterio de diseño). **Prioridad:** P1.

---

**F3-006 · componentes · P1**
**Vista:** fichas, rail izquierdo (acciones principales).
**Problema:** el CTA primario cambia de nombre Y de color por tipo: libro «+ Registrar sesión»
(naranja), serie «+ Marcar episodio» (**morado** — único botón morado de la app), película sin
pase «Seguir» (naranja). Y dentro de la serie conviven DOS CTAs para la misma acción con
distinto color y texto: «Marcar episodio» (morado, rail) y «✓ Marcar próximo episodio» (naranja,
pestaña Episodios).
**Evidencia:** capturas de The Hobbit, The Boys (rail + pestaña Episodios).
**Impacto:** la acción más importante de la app no tiene identidad estable.
**Patrón relacionado:** S5 (inventario de botones), F1-032 (CTA de película que apunta a un tab
sin botón).
**Propuesta:** un solo color de CTA primario; verbos consistentes por tipo («Registrar sesión» /
«Registrar visionado» / «Marcar episodio») decididos una vez y escritos en un glosario de UI.
**Complejidad:** baja. **Prioridad:** P1.

---

**F3-007 · funcional-UI · P2**
**Vista:** serie, pestaña Mi registro.
**Problema:** la pestaña muestra el bloque «SESIONES — Todavía no has registrado ninguna sesión»
con enlace «Registrar sesión»: vocabulario y mecánica de LIBRO en una serie, cuya unidad real es
el episodio. No hay «por dónde vas» (T/E) en todo el panel. Es la cara visible de F1-026.
**Evidencia:** captura de The Boys ?tab=log.
**Impacto:** confusión directa («¿qué es una sesión de una serie?») y paridad rota.
**Patrón relacionado:** F1-026, F3-005.
**Propuesta:** en serie, sustituir el bloque Sesiones por progreso de episodios (T2·E3 · 12/40)
con acceso a la pestaña Episodios; cablear las props que ya existen (F1-026) o registrar acta.
**Complejidad:** baja-media. **Prioridad:** P2.

---

**F3-008 · datos-en-UI · P2 (sospecha)**
**Vista:** serie The Boys, rail.
**Problema:** el rail dice estado «Vista» (pase completado) y a la vez «1 / 40 vistos · 3 %».
Estado y progreso se contradicen en la misma tarjeta.
**Evidencia:** captura del rail; en BD el pase activo es `completed` (2.º visionado) con 1
episodio visto.
**Impacto:** desconfianza en lo que dice la pantalla (familia F1-004/F1-005: el progreso de serie
tiene dos fórmulas y varios bugs conocidos).
**Patrón relacionado:** F1-004, F1-005.
**Propuesta:** decidir la semántica (¿el progreso mostrado es del pase activo o histórico?) al
arreglar F1-004; que el rail nunca muestre estado y progreso incoherentes.
**Complejidad:** la de F1-004. **Prioridad:** P2 (etiquetar `tipo:sospecha` al abrir issue).

### S3 · Navegación secundaria: cuatro familias para un solo problema

---

**F3-009 · componentes · P2**
**Vista:** transversal.
**Problema:** conviven CUATRO patrones de navegación secundaria: (a) tabs subrayados de ficha
(`?tab=`); (b) sidebar izquierda de club (+ mezcla `?tab=` con subruta /calendario, F1-032);
(c) sub-nav de anclas monospace de /estadisticas; (d) pills-que-parecen-tabs (Colección
Todo/Colecciones/Sagas; Sagas Todas/Las que sigo/Universos; Actividades EN CURSO/PRÓXIMAS/
HISTORIAL; filtros de feed TODO/RESEÑAS/...). La ficha de saga añade una barra de tabs con UNA
sola pestaña («Info»), vestigial.
**Evidencia:** capturas de ficha, club, estadísticas, colección, sagas, actividades, saga.
**Impacto:** el usuario re-aprende «cómo se cambia de sección» en cada dominio; los estilos
compiten visualmente.
**Patrón relacionado:** F1-032 (dos familias de tabs en código).
**Propuesta:** reducir a DOS patrones con semántica fija: tabs (cambian el contenido de la misma
entidad) y pills de filtro (filtran una lista). El sidebar de club puede quedarse como excepción
justificada (es un «espacio», no una entidad), pero con tabs internos consistentes. Quitar la
barra de un solo tab en saga.
**Complejidad:** media (component-level, no por página). **Prioridad:** P2.

---

**F3-010 · IA/descubribilidad · P1**
**Vista:** navegación global.
**Problema:** confirmación visual de F1-025: Cuaderno, Estadísticas, Sagas e Importar no cuelgan
de ninguna navegación; la «configuración» de la app es un modal de Editar perfil que además
esconde Importar/Exportar CSV; no existe página de ajustes; /cuenta/contrasena es huérfana. La
nav superior tiene 4 entradas y la app tiene ~8 áreas.
**Evidencia:** recorrido completo: a Cuaderno solo se llega desde una card del Rincón; a
Estadísticas desde la pestaña del perfil; a Sagas desde un enlace discreto en Buscar.
**Impacto:** features enteras invisibles (el daño funcional ya está en F1-025; aquí se confirma
que el problema es de ARQUITECTURA de navegación, no de un enlace suelto).
**Patrón relacionado:** F1-025.
**Propuesta:** decidir la IA de primer nivel (p. ej. Inicio · Biblioteca · Descubrir(Buscar+
Sagas+Géneros) · Clubes · Tú(Perfil+Cuaderno+Estadísticas+Ajustes)) y crear una página de
Ajustes real (perfil, contraseña, importar/exportar, notificaciones).
**Complejidad:** media. **Prioridad:** P1.

---

**F3-011 · naming · P2**
**Vista:** transversal.
**Problema:** el mismo concepto cambia de nombre según la vista: la página `/notas` se titula
«Cuaderno», la ficha dice «Mis notas y citas», el Rincón dice «Notas guardadas»; la nav dice
«Colección» y la página se titula «Mi Biblioteca»; «Sagas» agrupa sagas y «universos» y
«colecciones» (de estudio) mientras «Colecciones» (listas de usuario) vive en Biblioteca — dos
significados de «colección» a un clic de distancia.
**Evidencia:** capturas de /notas, ficha, rincón, /coleccion, /sagas.
**Impacto:** fricción de vocabulario constante; imposible construir un mapa mental.
**Patrón relacionado:** F3-010.
**Propuesta:** glosario de UI de una página (término canónico por concepto, ES) y barrido de
i18n; decidir «Biblioteca» vs «Colección» de una vez.
**Complejidad:** baja (el barrido) tras una decisión. **Prioridad:** P2.

### S4 · Acciones destructivas y jerarquía de acciones

---

**F3-012 · jerarquía · P1**
**Vista:** transversal (ficha Mi registro, notas, retos, feed de club, ediciones).
**Problema:** lo destructivo se presenta como texto-link inline con el MISMO peso que lo neutro,
sembrado por defecto en cada item: «Eliminar» junto a cada sesión, «Borrar pase» junto a
«Editar», «Quitar de mi biblioteca» como link suelto, «Borrar» rojo en cada nota y cada post del
club, «Archivar/Eliminar» en cada reto, y una **X de borrado siempre visible en cada tarjeta de
edición** de la ficha (para cualquier usuario logueado). En paralelo, /admin cambia roles con un
select sin confirmación («los cambios son inmediatos»).
**Evidencia:** capturas de Hobbit ?tab=log, /notas, Rincón, club feed, ediciones, /admin.
**Impacto:** riesgo de pérdida por misclick y ruido visual permanente; la jerarquía
primaria/secundaria/destructiva simplemente no existe como sistema.
**Patrón relacionado:** S5.
**Propuesta:** patrón único: destructivo NUNCA inline por defecto — vive tras un menú «···» (que
ya existe en colecciones y persona), con confirmación solo en lo irreversible (borrar pase,
borrar edición, rol de admin). Definirlo una vez en el sistema de componentes.
**Complejidad:** media (tocará muchos sitios, mecánico). **Prioridad:** P1.

### S5 · Sistema de componentes: cards, botones, estados

---

**F3-013 · componentes · P2**
**Vista:** todas las listas de obras.
**Problema:** la «card de obra» tiene ≥5 variantes no intencionadas: Colección (portada + pill de
estado + 2 icon-buttons SIEMPRE visibles que tapan la portada), Búsqueda (portada + badge «N
ediciones» — dato de curador, no de usuario), Género (portada + año pelado), Persona destacadas
(portada + año + rol), Continuar/Para más tarde (mini con barra), feed (thumbnail). Metadatos y
adornos distintos en cada una sin razón de contexto.
**Evidencia:** capturas de /coleccion, /buscar, /genero/fantasia, /persona, Inicio.
**Impacto:** la unidad básica de la interfaz (la portada) no es un componente: es seis.
**Patrón relacionado:** F1-020 (misma enfermedad en datos: N copias del mismo concepto).
**Propuesta:** UN `WorkCard` con slots (badge de estado, progreso, acciones on-hover — no
permanentes) y variantes por tamaño, documentado; el badge «N ediciones» fuera de resultados de
usuario.
**Complejidad:** media. **Prioridad:** P2.

---

**F3-014 · componentes · P2**
**Vista:** transversal.
**Problema:** inventario de botones sin sistema: naranja sólido, pill blanca, pill outline,
verde «Aprobar» (moderación de club), morado «Marcar episodio», link-caps «CONTINUAR →»,
«AMPLIAR ↗» monospace, text-links subrayados. Además el botón «Abrir» duplicado en cada card de
club (la card entera debería ser el link).
**Evidencia:** capturas de club/actividades, estadísticas, clubes, fichas.
**Impacto:** cada color nuevo diluye el significado del primario; suma a F3-006.
**Patrón relacionado:** F3-006, F3-012.
**Propuesta:** paleta de 4 roles (primario naranja / secundario outline / terciario texto /
destructivo) + regla «un primario por vista»; los links-caps quedan para navegación interna de
cards.
**Complejidad:** baja-media. **Prioridad:** P2.

---

**F3-015 · estados · P2**
**Vista:** transversal.
**Problema:** los estados vacíos son texto plano sin acción en casi toda la app: «Escribe algo
para buscar.», «No se encontraron clubes.» (sección Descubrir vacía), «Sin sinopsis
disponible.», «Nada marcado en este mes.», y —el peor— las colecciones sin ítems son tarjetas
BLANCAS COMPLETAMENTE VACÍAS en la rejilla de Colecciones. Hay dos buenos modelos ya en casa:
la card oscura «¿No sabes qué leer? → Sacar un lomo» del Rincón y el «Sin datos — los 10 puntos
medidos valen cero; no es que falten datos» de Estadísticas.
**Evidencia:** capturas de /buscar, /clubes, saga, calendario, Colecciones (rejilla).
**Impacto:** pantallas que parecen rotas o a medio cargar; oportunidades de acción perdidas
(exactamente lo que pide la spec de esta fase).
**Patrón relacionado:** S1 (los vacíos agravan la sensación de página desierta).
**Propuesta:** componente `EmptyState` (icono/ilustración + qué pasa + acción) obligatorio en
listas; mosaico placeholder para colecciones vacías.
**Complejidad:** baja-media. **Prioridad:** P2.

---

**F3-016 · componentes · P3**
**Vista:** feed / perfil / club.
**Problema:** metadatos de las cards de actividad sin patrón: timestamp arriba-derecha en
actividades de club, abajo-derecha en posts, agrupado por «13 AGO» en perfil; la card de post
anida una card beige (cita) dentro de la card blanca con ~90 % de aire cuando no hay texto;
reacciones/comentarios a veces dentro, a veces fuera del recuadro.
**Evidencia:** capturas de Inicio, perfil, club feed.
**Impacto:** bajo por pieza; en conjunto el feed se ve «desordenado» sin que se pueda señalar
por qué.
**Patrón relacionado:** F3-002.
**Propuesta:** anatomía única de entrada de feed (avatar+actor+verbo / cuerpo con media /
acciones+tiempo) aplicada a posts, hitos y actividades de club.
**Complejidad:** media. **Prioridad:** P3.

### S6 · Hallazgos puntuales observados de paso (bugs visibles)

---

**F3-017 · BUG visual · P2 (sospecha de datos o de etiqueta)**
«Test Public Club» luce badge «PRIVADO» en la lista de clubes Y en la cabecera del propio club.
O todos los clubes del seed son privados con nombre engañoso (datos), o el badge pinta siempre
«privado» (código). Verificar antes de arreglar. *Capturas: /clubes y /club/test-public-club.*

**F3-018 · BUG visual · P3**
La sinopsis de The Hobbit muestra Markdown crudo: «[**PDF**](https://chesserresources.com/...)».
Las sinopsis importadas no se sanean ni renderizan. Decidir: render markdown o strip al importar.

**F3-019 · BUG consola · P2**
Toda página con avatar emite el warning de Next: `Image ... has a "loader" property that does not
implement width` (el `cdn-loader` de `lib/images/` ignora `width`). Ruido en consola en TODAS las
vistas y señal de que el loader no está optimizando nada. Emparenta con la nota de F1-032 sobre
`lib/images/`.

**F3-020 · UX · P3**
Colección abre con «Filtros (1)» ya activo sin que el usuario haya filtrado (algún filtro por
defecto cuenta como activo). El contador miente sobre la intención del usuario.

**F3-021 · entorno · (nota, sin ID de diseño)**
Al arrancar, TODAS las rutas devolvían 404 con el layout renderizado; se resolvió borrando
`.next` (caché corrupta tras cambios de rama). Si reaparece: `Remove-Item -Recurse -Force .next`
antes de diagnosticar código. El puerto 3000 estaba ocupado por una sesión anterior (regla de
higiene del repo confirmada una vez más).

### Lo que está BIEN y hay que proteger

- **Identidad visual**: serif display + paleta papel/teja + portadas como material. Distintiva
  y consistente en color; el problema es de layout, no de estilo.
- **/persona**: el mejor layout desktop de la app (card de persona | obras con destacadas +
  cronología | rail de sagas). Es la plantilla a imitar en las fichas (F3-001).
- **/estadisticas**: densidad ejemplar (headline + micro-chart + AMPLIAR), sub-nav de anclas
  útil; solo le sobra la fila de «FILTROS» con aspecto de input.
- **Pestaña Episodios de serie**: sidebar de temporadas + lista con toggles Lista/Rejilla y
  Mis notas/Comunidad — exactamente el tipo de diseño por-tipo que las fichas necesitan.
- **Mapa de saga**: canvas oscuro full-bleed con leyenda; buen uso del espacio y buen contraste
  de «mundo aparte».
- **El editor de catálogo inline** con banner «Editando la ficha oficial» + barra sticky
  Guardar/Cancelar: patrón de edición claro (solo le falta atenuar el rail personal mientras).

### Principios UI propuestos (candidatos a estándar)

1. **Desktop se diseña a dos columnas mínimo.** Toda página con contenido principal + contexto
   usa main+sidebar (o 3 columnas si hay actividad); una sola columna centrada queda reservada a
   formularios utilitarios (max-width ~720px). Ninguna página deja un tercio del viewport vacío
   de forma sistemática.
2. **Una plantilla de ficha, tres inserciones.** Libro, película y serie comparten orden de
   secciones, pestañas y rail; cada tipo solo aporta sus secciones propias en huecos reservados.
   Toda divergencia entre tipos es una decisión registrada, nunca un accidente.
3. **Un primario por vista, un color de primario en la app.** El CTA principal es naranja y hay
   exactamente uno visible por pantalla; secundarios outline, terciarios texto. Ningún color
   nuevo de botón sin entrada en `decisiones.md`.
4. **Lo destructivo nunca vive inline.** Borrar/quitar/archivar van tras un menú «···» con
   confirmación si es irreversible. Un item de lista muestra como máximo una acción secundaria
   permanente.
5. **Dos patrones de navegación secundaria.** Tabs = misma entidad, distinto contenido; pills =
   filtros de una lista. Nada de tabs de un solo elemento ni pills que naveguen a rutas.
6. **La portada es EL componente.** Un solo `WorkCard` con variantes de tamaño y slots
   (estado, progreso, acciones on-hover); toda lista de obras lo usa. Las acciones no tapan la
   portada en reposo.
7. **Todo vacío explica y ofrece.** `EmptyState` estándar: qué pasa aquí + qué puedes hacer
   (con CTA cuando exista). Prohibido el string suelto y la card en blanco.
8. **Un nombre por concepto.** Glosario de UI canónico (Biblioteca, Cuaderno, Pase, Saga,
   Universo, Lista...); i18n y páginas usan el término del glosario o no pasan review.
9. **Toda feature cuelga de la navegación.** Si una ruta no es alcanzable desde la nav de primer
   nivel o desde su área, o se le da entrada o se registra acta de por qué es contextual.
   (Convierte F1-025 en regla permanente.)
10. **Los números de la pantalla no se contradicen.** Estado y progreso mostrados juntos deben
    derivar del mismo dato (una sola fórmula por métrica — regla ya implícita en F1-004/F3-008).

### Orden de ataque sugerido para el rediseño (fase posterior)

| # | Paquete | Cubre | Coste |
|---|---|---|---|
| 1 | Sistema mínimo: roles de botón + acciones destructivas + EmptyState + glosario | F3-006, F3-012, F3-014, F3-015, F3-011 | medio |
| 2 | Plantilla de ficha 2-col unificada (con el refactor F1-021) | F3-001, F3-005, F3-007 | alto |
| 3 | IA de navegación + página de Ajustes | F3-010, F1-025 | medio |
| 4 | WorkCard único + pasada de cards de feed | F3-013, F3-016, F3-002 | medio |
| 5 | Utilitarias (buscar, importar, admin, contraseña) | F3-003, F3-004 | bajo |

> Los bugs S6 (F3-017 a F3-020) se abren como issues sueltas ya — no esperan al rediseño.
