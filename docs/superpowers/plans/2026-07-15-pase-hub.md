# El pase como hub — plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrar el estado, el cursor, la cola y los fijados desde `library_entries` al pase (`diary_entries` → `passes`), con máquina de transiciones única, auto-cierre, hoja de retomar y cursor por pase en series.

**Architecture:** Migración expand-first en tres tandas SQL (expandir → re-anclar satélites → renombrar), con el código cambiando de fuente entre las tandas B y C para que cada tarea deje el árbol compilando. `library_entries` queda huérfana en solo-lectura como red de revert. Una función pura `planTransition` decide TODOS los cambios de estado; un ejecutor `applyTransition` los aplica; nadie más escribe `status`.

**Tech Stack:** Next.js App Router + Supabase (Postgres, RLS con grants por columna), Vitest, Playwright.

**Spec:** `docs/superpowers/specs/2026-07-15-pase-hub-design.md`

## Global Constraints

- Migraciones: ficheros en `supabase/migrations/`. **El MCP de Supabase está clavado a PROD** (`vmutcradmodhiltuohys`): NUNCA uses `mcp__supabase__apply_migration` / `execute_sql` durante el desarrollo. DEV es `tyvzpuhxfwxrnkcpzxyg` y se toca por Management API: `POST https://api.supabase.com/v1/projects/tyvzpuhxfwxrnkcpzxyg/database/query` con header `Authorization: Bearer $SUPABASE_ACCESS_TOKEN` (está en el shell) y body `{"query":"..."}`; `jq` no existe — construye el JSON con `node -e`. Prod solo al final, tras merge (Task 12), ahí sí vía MCP. Tipos: `npx supabase gen types typescript --project-id tyvzpuhxfwxrnkcpzxyg` (CLI, no MCP).
- La BD usa **grants por columna** en `diary_entries` (la columna `review` no es legible desde el cliente): cada columna nueva necesita su `grant select` explícito, y `review` se lee SIEMPRE por la vista `pass_reviews`.
- Vitest necesita Node 22: `fnm use 22.23.1` antes de `npx vitest run` (el shell arranca en 20.9).
- La palabra "pase" no aparece en la interfaz; los textos de UI van por next-intl (`messages/es.json`, `messages/en.json`).
- Nada de `git stash` a pelo (stack compartido entre worktrees); commits frecuentes en la rama `worktree-pase-hub-spec`.
- Enum de estados existente: `media_status` = `planned · in_progress · completed · dropped`. No se crea enum nuevo.
- Convención de nombres BD en inglés: `is_active`, no `activo`.

---

### Task 1: Migración A — el pase absorbe el hub

**Files:**
- Create: `supabase/migrations/20260716_pass_hub_a_columns.sql`

**Interfaces:**
- Produces: columnas `item_type, item_id, status, is_active, position, queue_id, queue_order, pinned_order` en `diary_entries`; índice único `passes_one_active`; vista `pass_reviews` ampliada con esas columnas. Todo lo posterior depende de este esquema.

- [ ] **Step 1: Escribir la migración**

```sql
-- El pase pasa a ser el hub: absorbe estado, cursor, cola y fijados de
-- library_entries. library_entries NO se toca aún (queda como red de revert;
-- se congela en la migración C). Ver docs/superpowers/specs/2026-07-15-pase-hub-design.md.

-- 1) Columnas nuevas.
alter table public.diary_entries
  add column if not exists item_type public.item_type,
  add column if not exists item_id uuid,
  add column if not exists status public.media_status not null default 'planned',
  add column if not exists is_active boolean not null default false,
  add column if not exists position jsonb not null default '{}'::jsonb,
  add column if not exists queue_id uuid references public.queues(id) on delete set null,
  add column if not exists queue_order integer,
  add column if not exists pinned_order integer;

-- 2) Toda entrada sin ningún pase engendra uno, para que "en mi biblioteca"
--    pueda pasar a significar "existe pase activo". Idempotente.
insert into public.diary_entries (library_entry_id, user_id, started_on, finished_on, is_public)
select
  le.id,
  le.user_id,
  case when le.status <> 'planned'
       then coalesce(le.started_at::date, le.updated_at::date) end,
  case when le.status in ('completed', 'dropped') then le.updated_at::date end,
  true
from public.library_entries le
where not exists (
  select 1 from public.diary_entries d where d.library_entry_id = le.id
);

-- 3) La obra, en todos los pases (aún vía library_entry_id; la columna se
--    volverá not null al final de este fichero).
update public.diary_entries d
set item_type = le.item_type, item_id = le.item_id
from public.library_entries le
where d.library_entry_id = le.id
  and (d.item_type is null or d.item_id is null);

-- 4) Estado provisional derivado de finished_on. El histórico no distinguía
--    completado de abandonado: los cerrados no-activos quedan como
--    'completed' (es lo que la media de comunidad asumía de facto).
update public.diary_entries set status = 'completed'
where finished_on is not null and status = 'planned';
update public.diary_entries set status = 'in_progress'
where finished_on is null and status = 'planned' and started_on is not null;

-- 5) El pase activo de cada entrada (el abierto si lo hay; si no, el último
--    cerrado) hereda el estado REAL y el contexto de biblioteca.
with actives as (
  select distinct on (d.library_entry_id) d.id, d.library_entry_id
  from public.diary_entries d
  order by d.library_entry_id,
           (d.finished_on is null) desc,
           d.finished_on desc,
           d.created_at desc
)
update public.diary_entries d
set is_active   = true,
    status      = le.status,
    position    = le.position,
    queue_id    = le.queue_id,
    queue_order = le.queue_order,
    pinned_order = le.pinned_order
from actives a
join public.library_entries le on le.id = a.library_entry_id
where d.id = a.id;

-- 6) Coherencia estado ↔ fechas en los activos (deriva histórica posible):
--    cerrado sin fecha gana la fecha; abierto con fecha la pierde. El índice
--    diary_entries_one_open_pass no puede chocar: si hubiera habido un pase
--    abierto, ESE habría sido elegido activo en el paso 5.
update public.diary_entries
set finished_on = coalesce(finished_on, updated_at::date)
where is_active and status in ('completed', 'dropped') and finished_on is null;
update public.diary_entries
set finished_on = null
where is_active and status in ('planned', 'in_progress') and finished_on is not null;

-- 7) Cierres e índices.
alter table public.diary_entries
  alter column item_type set not null,
  alter column item_id set not null;

create unique index if not exists passes_one_active
  on public.diary_entries (user_id, item_type, item_id)
  where is_active;
create index if not exists passes_active_by_user
  on public.diary_entries (user_id)
  where is_active;
create index if not exists passes_by_item
  on public.diary_entries (item_type, item_id);

-- 8) Grants por columna: el SELECT de tabla se quitó en
--    20260714_passes_review_privacy.sql; cada columna nueva necesita el suyo.
grant select (item_type, item_id, status, is_active, position,
              queue_id, queue_order, pinned_order)
  on public.diary_entries to anon, authenticated;

-- 9) La vista de reseñas gana las columnas nuevas (mismo WHERE de privacidad).
--    OJO: create or replace view no admite insertar columnas en medio de la
--    lista — hay que soltarla y recrearla (y re-conceder el grant).
--    El literal 'diary_entries' de is_visible_via_club_share es la etiqueta
--    almacenada en las comparticiones de club: NO cambia aunque la tabla se
--    renombre después (dato, no nombre de tabla).
drop view if exists public.pass_reviews;
create view public.pass_reviews as
select
  d.id, d.library_entry_id, d.user_id, d.item_type, d.item_id,
  d.status, d.is_active, d.position, d.pinned_order,
  d.started_on, d.finished_on, d.rating, d.review, d.is_public,
  d.edition_id, d.created_at
from public.diary_entries d
where
  d.user_id = (select auth.uid())
  or (
    d.is_public
    and (
      public.can_view_profile(d.user_id)
      or public.is_visible_via_club_share('diary_entries', d.id, d.user_id)
    )
  );

grant select on public.pass_reviews to anon, authenticated;
```

- [ ] **Step 2: Consultas de control ANTES de aplicar (dev)**

Vía Management API sobre **dev** (ver Global Constraints: NUNCA el MCP, que apunta a prod), guarda los resultados:

```sql
select count(*) as entradas from library_entries;
select count(*) as pases from diary_entries;
select count(*) as notas from diary_entries where rating is not null;
-- media actual por obra (fórmula vieja: pases cerrados con nota, entrada no dropped)
select le.item_type, le.item_id, round(avg(d.rating)::numeric, 3) as media, count(*) as votos
from diary_entries d join library_entries le on le.id = d.library_entry_id
where d.finished_on is not null and d.rating is not null and le.status <> 'dropped'
group by 1, 2 order by 1, 2;
```

- [ ] **Step 3: Aplicar la migración en dev** vía Management API (el contenido del fichero SQL como `query`).

- [ ] **Step 4: Consultas de control DESPUÉS**

```sql
-- exactamente un activo por entrada
select (select count(*) from library_entries) =
       (select count(*) from diary_entries where is_active) as activos_ok;
-- ningún pase sin obra
select count(*) = 0 as obra_ok from diary_entries where item_id is null;
-- estado del activo = estado de la entrada
select count(*) = 0 as estados_ok
from diary_entries d join library_entries le on le.id = d.library_entry_id
where d.is_active and d.status <> le.status;
-- notas intactas
select count(*) as notas from diary_entries where rating is not null;
-- media nueva por obra (fórmula nueva: status del pase, sin join)
select item_type, item_id, round(avg(rating)::numeric, 3) as media, count(*) as votos
from diary_entries
where finished_on is not null and rating is not null and status <> 'dropped'
group by 1, 2 order by 1, 2;
```

La media nueva debe coincidir con la vieja. Única divergencia admisible: un usuario con la entrada HOY en `dropped` pero con un pase cerrado antiguo con nota — la fórmula vieja lo excluía por entrada, la nueva solo excluye el pase abandonado (es lo que la spec de PR #32 siempre quiso). Si aparecen filas distintas, verifica que TODAS responden a ese patrón; si no, para y revierte.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260716_pass_hub_a_columns.sql
git commit -m "feat(db): el pase absorbe estado, cursor, cola y fijados (hub, tanda A)"
```

---

### Task 2: Migración B — sesiones y episodios cuelgan del pase

**Files:**
- Create: `supabase/migrations/20260716_pass_hub_b_satellites.sql`

**Interfaces:**
- Consumes: pases activos de la Task 1.
- Produces: `progress_sessions.pass_id` not null; `episode_watches.pass_id` (nullable; null = capa "visto alguna vez" sin pase).

- [ ] **Step 1: Escribir la migración**

```sql
-- Las sesiones pasan a colgar SOLO del pase; los episodios vistos ganan
-- pass_id para que el cursor de serie sea por pase (revisionados con cursor
-- propio). pass_id null en episode_watches = visto en la era pre-pases o de
-- una serie ya quitada de la biblioteca: cuenta para "visto alguna vez",
-- no para el cursor de ningún pase.

-- 1) Sesiones huérfanas de pase → al pase activo de su entrada.
update public.progress_sessions s
set pass_id = d.id
from public.diary_entries d
where s.pass_id is null
  and d.library_entry_id = s.library_entry_id
  and d.is_active;

alter table public.progress_sessions alter column pass_id set not null;

-- 2) Episodios vistos → al pase activo de esa serie para ese usuario.
alter table public.episode_watches
  add column if not exists pass_id uuid references public.diary_entries(id) on delete cascade;

update public.episode_watches w
set pass_id = d.id
from public.diary_entries d
where w.pass_id is null
  and d.user_id = w.user_id
  and d.item_type = 'series'
  and d.item_id = w.series_id
  and d.is_active;

create index if not exists episode_watches_by_pass
  on public.episode_watches (pass_id);
```

- [ ] **Step 2: Aplicar en dev** vía Management API (ver Global Constraints).

- [ ] **Step 3: Control**

```sql
select count(*) = 0 as sesiones_ok from progress_sessions where pass_id is null;
-- vistos sin pase: solo admisible si el usuario no tiene la serie en biblioteca
select count(*) as vistos_sin_pase from episode_watches where pass_id is null;
```

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260716_pass_hub_b_satellites.sql
git commit -m "feat(db): sesiones y episodios vistos cuelgan del pase (hub, tanda B)"
```

---

### Task 3: Tipos regenerados y capa de lectura de pases

**Files:**
- Modify: `src/lib/supabase/database.types.ts` (regenerado)
- Modify: `src/lib/passes/types.ts`
- Modify: `src/lib/passes/get-passes.ts`

**Interfaces:**
- Produces: tipo `Pass` ampliado — `{ id, status: MediaStatus, isActive: boolean, position: Json, startedOn, finishedOn, rating, review, isPublic, editionId }`; `getPasses(supabase, itemType, itemId)` (por obra, ya no por entrada) y `getActivePass(supabase, itemType, itemId)`. Los consumidores actuales de `getPasses(supabase, entryId)` se corrigen en las tareas 5–8; hasta entonces el build puede tener errores de tipos SOLO en esos ficheros — anótalos, no los "arregles" a medias.

- [ ] **Step 1: Regenerar tipos** con `npx supabase gen types typescript --project-id tyvzpuhxfwxrnkcpzxyg > src/lib/supabase/database.types.ts` (CLI con `$SUPABASE_ACCESS_TOKEN`; NO el MCP, que apunta a prod).

- [ ] **Step 2: Ampliar `Pass` en `src/lib/passes/types.ts`**

```ts
import type { MediaStatus } from "@/lib/library/types";
import type { Json } from "@/lib/supabase/database.types";

// Un "pase" es una lectura o un visionado, y desde la migración hub es el
// dueño de TODO el registro personal: estado, cursor, cola, fijado, nota y
// reseña. "Activo" = el que representa la obra en tu biblioteca (puede estar
// cerrado). Abierto = planned | in_progress.
export type Pass = {
  id: string;
  status: MediaStatus;
  isActive: boolean;
  position: Json;
  startedOn: string | null;
  finishedOn: string | null;
  // Nota SIEMPRE entera 1-10; la escala de estrellas es solo presentación.
  rating: number | null;
  review: string | null;
  isPublic: boolean;
  editionId: string | null;
  // Solo significativo en el pase activo; lo hereda el pase nuevo al archivar.
  pinnedOrder: number | null;
};
```

- [ ] **Step 3: `getPasses` por obra + `getActivePass`**

```ts
import type { createClient } from "@/lib/supabase/server";
import type { ItemType } from "@/lib/catalog/types";
import type { Pass } from "./types";

type SupabaseServerClient = Awaited<ReturnType<typeof createClient>>;

// Pases del usuario para una obra: el abierto primero, luego cerrados de más
// reciente a más antiguo (orden que espera el diario para el delta). review
// se lee SIEMPRE por la vista pass_reviews (privacidad aplicada); como la
// vista solo enseña filas visibles, para el diario propio devuelve todo.
export async function getPasses(
  supabase: SupabaseServerClient,
  itemType: ItemType,
  itemId: string,
  userId: string
): Promise<Pass[]> {
  const { data } = await supabase
    .from("pass_reviews")
    .select(
      "id, status, is_active, position, started_on, finished_on, rating, review, is_public, edition_id, pinned_order"
    )
    .eq("user_id", userId)
    .eq("item_type", itemType)
    .eq("item_id", itemId)
    .order("finished_on", { ascending: false, nullsFirst: true });

  return (data ?? []).map((r) => ({
    id: r.id as string,
    status: r.status as Pass["status"],
    isActive: r.is_active as boolean,
    position: r.position as Pass["position"],
    startedOn: r.started_on,
    finishedOn: r.finished_on,
    rating: r.rating,
    review: r.review,
    isPublic: r.is_public as boolean,
    editionId: r.edition_id,
    pinnedOrder: r.pinned_order,
  }));
}

export async function getActivePass(
  supabase: SupabaseServerClient,
  itemType: ItemType,
  itemId: string,
  userId: string
): Promise<Pass | null> {
  const passes = await getPasses(supabase, itemType, itemId, userId);
  return passes.find((p) => p.isActive) ?? null;
}
```

- [ ] **Step 4: Compilar y anotar** — `npx tsc --noEmit`. Espera errores SOLO en consumidores de `getPasses`/`Pass` que se reescriben en tareas 5–8 (`src/lib/passes/actions.ts`, `src/lib/sessions/actions.ts`, `src/app/sesion/[entryId]/page.tsx`, `src/lib/library/manage-actions.ts`, componentes de detalle). Lista los ficheros con error en el mensaje de commit.

- [ ] **Step 5: Commit**

```bash
git add src/lib/supabase/database.types.ts src/lib/passes/types.ts src/lib/passes/get-passes.ts
git commit -m "feat(passes): tipos del hub y lectura de pases por obra"
```

---

### Task 4: La máquina de transiciones

**Files:**
- Modify: `src/lib/passes/transitions.ts` (reescrito; `passEffect` se conserva exportado hasta la Task 5)
- Modify: `src/lib/passes/transitions.test.ts`

**Interfaces:**
- Produces:

```ts
export type ActivePassSnapshot = { id: string; status: MediaStatus };
export type Transition =
  | { kind: "none" }
  | { kind: "createActive"; status: MediaStatus; startedOn: string | null; finishedOn: string | null }
  | { kind: "updateActive"; set: { status: MediaStatus; started_on?: string; finished_on?: string | null } }
  | { kind: "archiveAndCreate"; status: MediaStatus; startedOn: string | null }
  | { kind: "askResume" };
export function planTransition(
  active: ActivePassSnapshot | null,
  to: MediaStatus,
  today: string,
  resume?: "continue" | "restart"
): Transition;
```

- Reglas de negocio que codifica (y que las tareas 5–8 dan por hechas): abrir/cerrar hoja de cierre y limpiar cola NO son cosa suya — eso lo decide el ejecutor mirando la transición devuelta.

- [ ] **Step 1: Escribir los tests que fallan** (sustituyen a los de `passEffect`, que se borran en la Task 5; añade estos al final del fichero)

```ts
import { describe, it, expect } from "vitest";
import { planTransition } from "./transitions";

const HOY = "2026-07-16";

describe("planTransition", () => {
  // Sin pase activo: añadir a biblioteca / película vista de golpe
  it("sin activo, a planned → crea activo pendiente", () => {
    expect(planTransition(null, "planned", HOY)).toEqual({
      kind: "createActive", status: "planned", startedOn: null, finishedOn: null,
    });
  });
  it("sin activo, a in_progress → crea activo leyendo", () => {
    expect(planTransition(null, "in_progress", HOY)).toEqual({
      kind: "createActive", status: "in_progress", startedOn: HOY, finishedOn: null,
    });
  });
  it("sin activo, a completed → crea activo ya cerrado (película vista)", () => {
    expect(planTransition(null, "completed", HOY)).toEqual({
      kind: "createActive", status: "completed", startedOn: HOY, finishedOn: HOY,
    });
  });

  // Mismo estado = no-op
  it("mismo estado → none", () => {
    expect(planTransition({ id: "p1", status: "in_progress" }, "in_progress", HOY))
      .toEqual({ kind: "none" });
  });

  // Pase abierto
  it("planned → in_progress sella started_on en el MISMO pase", () => {
    expect(planTransition({ id: "p1", status: "planned" }, "in_progress", HOY)).toEqual({
      kind: "updateActive", set: { status: "in_progress", started_on: HOY },
    });
  });
  it("in_progress → completed cierra el pase", () => {
    expect(planTransition({ id: "p1", status: "in_progress" }, "completed", HOY)).toEqual({
      kind: "updateActive", set: { status: "completed", finished_on: HOY },
    });
  });
  it("planned → completed cierra de golpe (película)", () => {
    expect(planTransition({ id: "p1", status: "planned" }, "completed", HOY)).toEqual({
      kind: "updateActive", set: { status: "completed", started_on: HOY, finished_on: HOY },
    });
  });
  it("in_progress → dropped congela el cursor y cierra", () => {
    expect(planTransition({ id: "p1", status: "in_progress" }, "dropped", HOY)).toEqual({
      kind: "updateActive", set: { status: "dropped", finished_on: HOY },
    });
  });
  it("in_progress → planned reabre como pendiente sin perder started_on", () => {
    expect(planTransition({ id: "p1", status: "in_progress" }, "planned", HOY)).toEqual({
      kind: "updateActive", set: { status: "planned" },
    });
  });

  // Pase cerrado: relectura y retomar
  it("completed → in_progress = relectura: archiva y abre pase nuevo", () => {
    expect(planTransition({ id: "p1", status: "completed" }, "in_progress", HOY)).toEqual({
      kind: "archiveAndCreate", status: "in_progress", startedOn: HOY,
    });
  });
  it("completed → planned = quiero releerlo: archiva y abre pendiente", () => {
    expect(planTransition({ id: "p1", status: "completed" }, "planned", HOY)).toEqual({
      kind: "archiveAndCreate", status: "planned", startedOn: null,
    });
  });
  it("dropped → in_progress sin elección → pide la hoja de retomar", () => {
    expect(planTransition({ id: "p1", status: "dropped" }, "in_progress", HOY))
      .toEqual({ kind: "askResume" });
  });
  it("dropped → in_progress con 'continue' reabre el MISMO pase", () => {
    expect(
      planTransition({ id: "p1", status: "dropped" }, "in_progress", HOY, "continue")
    ).toEqual({
      kind: "updateActive", set: { status: "in_progress", finished_on: null },
    });
  });
  it("dropped → in_progress con 'restart' archiva y abre de cero", () => {
    expect(
      planTransition({ id: "p1", status: "dropped" }, "in_progress", HOY, "restart")
    ).toEqual({ kind: "archiveAndCreate", status: "in_progress", startedOn: HOY });
  });
  it("dropped → completed corrige el cierre en el mismo pase", () => {
    expect(planTransition({ id: "p1", status: "dropped" }, "completed", HOY)).toEqual({
      kind: "updateActive", set: { status: "completed", finished_on: HOY },
    });
  });
});
```

- [ ] **Step 2: Verificar que fallan** — `fnm use 22.23.1 && npx vitest run src/lib/passes/transitions.test.ts`. Esperado: FAIL (`planTransition` no existe).

- [ ] **Step 3: Implementar `planTransition`** (añadir al fichero, SIN borrar `passEffect` todavía)

```ts
export type ActivePassSnapshot = { id: string; status: MediaStatus };

export type Transition =
  | { kind: "none" }
  | { kind: "createActive"; status: MediaStatus; startedOn: string | null; finishedOn: string | null }
  | { kind: "updateActive"; set: { status: MediaStatus; started_on?: string; finished_on?: string | null } }
  | { kind: "archiveAndCreate"; status: MediaStatus; startedOn: string | null }
  | { kind: "askResume" };

const OPEN: MediaStatus[] = ["planned", "in_progress"];

// La única fuente de verdad de los cambios de estado. TODA escritura de
// status pasa por aquí: ficha, formulario de sesión, auto-cierre. La hoja de
// cierre y la limpieza de cola las decide el ejecutor a partir del resultado.
export function planTransition(
  active: ActivePassSnapshot | null,
  to: MediaStatus,
  today: string,
  resume?: "continue" | "restart"
): Transition {
  if (!active) {
    if (to === "planned") return { kind: "createActive", status: to, startedOn: null, finishedOn: null };
    if (to === "in_progress") return { kind: "createActive", status: to, startedOn: today, finishedOn: null };
    // completed o dropped sin pase previo: nace ya cerrado (película vista).
    return { kind: "createActive", status: to, startedOn: today, finishedOn: today };
  }

  if (active.status === to) return { kind: "none" };

  if (OPEN.includes(active.status)) {
    if (to === "in_progress") return { kind: "updateActive", set: { status: to, started_on: today } };
    if (to === "planned") return { kind: "updateActive", set: { status: to } };
    if (to === "completed" && active.status === "planned")
      return { kind: "updateActive", set: { status: to, started_on: today, finished_on: today } };
    return { kind: "updateActive", set: { status: to, finished_on: today } };
  }

  // Activo cerrado (completed | dropped).
  if (to === "planned") return { kind: "archiveAndCreate", status: to, startedOn: null };
  if (to === "in_progress") {
    if (active.status === "dropped") {
      if (!resume) return { kind: "askResume" };
      if (resume === "continue")
        return { kind: "updateActive", set: { status: to, finished_on: null } };
    }
    return { kind: "archiveAndCreate", status: to, startedOn: today };
  }
  // dropped → completed (o viceversa): corrección sobre el mismo pase.
  return { kind: "updateActive", set: { status: to, finished_on: today } };
}
```

- [ ] **Step 4: Verificar que pasan** — `npx vitest run src/lib/passes/transitions.test.ts`. Esperado: PASS (los tests viejos de `passEffect` siguen pasando también).

- [ ] **Step 5: Commit**

```bash
git add src/lib/passes/transitions.ts src/lib/passes/transitions.test.ts
git commit -m "feat(passes): planTransition, la maquina de estados del pase"
```

---

### Task 5: El ejecutor y la ficha — nadie más escribe `status`

**Files:**
- Create: `src/lib/passes/apply-transition.ts`
- Modify: `src/lib/library/manage-actions.ts`
- Modify: `src/lib/passes/transitions.ts` (borrar `passEffect`) y `transitions.test.ts` (borrar sus tests)
- Modify: `src/lib/passes/actions.ts` (adaptar `openPass`; el resto de acciones —`closePass`, `updatePass`, `deletePass`, `ratePass`, `setPassEdition`— cambian el filtro `library_entry_id` por `id` de pase donde aplique y nada más)
- Modify: `src/lib/library/add-existing-item.ts`, `src/app/buscar/actions.ts`, `src/app/buscar/manual/actions.ts` (alta = crear pase activo planned en vez de `library_entries`)

**Interfaces:**
- Consumes: `planTransition`, `getActivePass` (Task 3/4).
- Produces:

```ts
// apply-transition.ts — helper de servidor (NO "use server"; lo llaman acciones)
export type TransitionOutcome =
  | { kind: "done"; passId: string; closed: boolean } // closed=true → la UI encadena la hoja de cierre
  | { kind: "askResume" };
export async function applyTransition(
  supabase: SupabaseServerClient,
  userId: string,
  itemType: ItemType,
  itemId: string,
  to: MediaStatus,
  resume?: "continue" | "restart"
): Promise<TransitionOutcome>;
```

- `updateStatus(itemType, itemId, status, resume?)` — pierde `entryId`; devuelve `TransitionOutcome` para que el cliente abra la hoja de retomar o la de cierre.

- [ ] **Step 1: Escribir `apply-transition.ts`**

```ts
import type { createClient } from "@/lib/supabase/server";
import type { ItemType } from "@/lib/catalog/types";
import type { MediaStatus } from "@/lib/library/types";
import { planTransition } from "./transitions";
import { getActivePass } from "./get-passes";

type SupabaseServerClient = Awaited<ReturnType<typeof createClient>>;

export type TransitionOutcome =
  | { kind: "done"; passId: string; closed: boolean }
  | { kind: "askResume" };

function today(): string {
  return new Date().toISOString().slice(0, 10);
}

// Ejecuta lo que planTransition decida. Reglas del ejecutor (§7.22 + spec):
// salir de planned limpia la cola; archivar hereda el fijado en el pase
// nuevo; el resultado dice si el pase quedó cerrado para que la UI encadene
// la hoja de cierre.
export async function applyTransition(
  supabase: SupabaseServerClient,
  userId: string,
  itemType: ItemType,
  itemId: string,
  to: MediaStatus,
  resume?: "continue" | "restart"
): Promise<TransitionOutcome> {
  const active = await getActivePass(supabase, itemType, itemId, userId);
  const plan = planTransition(
    active ? { id: active.id, status: active.status } : null,
    to,
    today(),
    resume
  );

  const clearQueue = to !== "planned" ? { queue_id: null, queue_order: null } : {};

  if (plan.kind === "askResume") return { kind: "askResume" };

  if (plan.kind === "none") {
    return { kind: "done", passId: active!.id, closed: false };
  }

  if (plan.kind === "updateActive") {
    const { error } = await supabase
      .from("diary_entries")
      .update({ ...plan.set, ...clearQueue })
      .eq("id", active!.id)
      .eq("user_id", userId);
    // 23505 = choque con passes_one_open / one_pass_per_day por doble click o
    // dos pestañas: el invariante hizo su trabajo, no es un 500.
    if (error && error.code !== "23505") throw error;
    return {
      kind: "done",
      passId: active!.id,
      closed: to === "completed" || to === "dropped",
    };
  }

  // createActive y archiveAndCreate comparten el insert.
  if (plan.kind === "archiveAndCreate") {
    const { error: archiveError } = await supabase
      .from("diary_entries")
      .update({ is_active: false, queue_id: null, queue_order: null })
      .eq("id", active!.id)
      .eq("user_id", userId);
    if (archiveError) throw archiveError;
  }

  const startedOn = plan.startedOn;
  const finishedOn = plan.kind === "createActive" ? plan.finishedOn : null;
  const { data: created, error } = await supabase
    .from("diary_entries")
    .insert({
      user_id: userId,
      item_type: itemType,
      item_id: itemId,
      status: plan.status,
      is_active: true,
      position: {},
      started_on: startedOn,
      finished_on: finishedOn,
      is_public: true,
      // El fijado es de la relación con la obra: lo hereda el pase nuevo.
      pinned_order: plan.kind === "archiveAndCreate" ? (active!.pinnedOrder ?? null) : null,
    })
    .select("id")
    .single();
  if (error && error.code !== "23505") throw error;
  return {
    kind: "done",
    passId: created?.id ?? active?.id ?? "",
    closed: plan.status === "completed" || plan.status === "dropped",
  };
}
```

(`Pass.pinnedOrder` viene definido en la Task 3.)

- [ ] **Step 2: Reescribir `updateStatus` en `manage-actions.ts`**

```ts
export async function updateStatus(
  itemType: ItemType,
  itemId: string,
  status: MediaStatus,
  resume?: "continue" | "restart"
): Promise<TransitionOutcome> {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) redirect("/login");

  const outcome = await applyTransition(supabase, user.id, itemType, itemId, status, resume);
  if (outcome.kind === "done") revalidateReadingLog(itemType, itemId);
  return outcome;
}
```

`removeFromLibrary` pasa a borrar TODOS los pases de la obra (con la confirmación que ya existe en la UI):

```ts
export async function removeFromLibrary(itemType: ItemType, itemId: string) {
  // ... auth idéntico ...
  const { error } = await supabase
    .from("diary_entries")
    .delete()
    .eq("user_id", user.id)
    .eq("item_type", itemType)
    .eq("item_id", itemId);
  if (error) throw error;
  revalidateReadingLog(itemType, itemId);
}
```

`moveEntryToQueue` opera sobre el pase activo planned:

```ts
export async function moveEntryToQueue(
  itemType: ItemType,
  itemId: string,
  queueId: string | null
) {
  // ... auth idéntico ...
  const { error } = await supabase
    .from("diary_entries")
    .update({ queue_id: queueId, queue_order: null })
    .eq("user_id", user.id)
    .eq("item_type", itemType)
    .eq("item_id", itemId)
    .eq("is_active", true)
    .eq("status", "planned");
  if (error) throw error;
  revalidateReadingLog(itemType, itemId);
  revalidateLibrary();
}
```

Actualiza los call sites de estas tres acciones (los componentes de detalle les pasaban `entryId`): búscalos con `grep -r "updateStatus\|removeFromLibrary\|moveEntryToQueue" src/components src/app` y quita el primer argumento.

- [ ] **Step 3: Alta en biblioteca = pase pendiente.** En `add-existing-item.ts` y los dos `buscar/*/actions.ts`, el `insert` en `library_entries` se sustituye por `applyTransition(supabase, user.id, itemType, itemId, "planned")` (idempotente: si ya hay activo, `none`). Elimina de esos ficheros cualquier lectura de `library_entries` para "¿ya lo tengo?" — ahora es `getActivePass(...) !== null`.

- [ ] **Step 4: Borrar `passEffect`** de `transitions.ts` y sus tests de `transitions.test.ts`; en `src/lib/passes/actions.ts` elimina `openPass` (lo sustituye `applyTransition`) y ajusta los filtros de las demás acciones (`.eq("library_entry_id", entryId)` → `.eq("id", passId)` con el id del pase que ya reciben, o el filtro por obra que corresponda).

- [ ] **Step 5: Compilar y probar** — `npx tsc --noEmit` (errores solo en sesiones/series/lectores pendientes, tareas 6–9) y `npx vitest run src/lib/passes`. Esperado: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A src/lib src/app src/components
git commit -m "feat(passes): applyTransition ejecuta la maquina; alta, estado, cola y borrado sobre pases"
```

---

### Task 6: Hojas de retomar y de cierre encadenado

**Files:**
- Create: `src/components/detail/resume-pass-sheet.tsx`
- Modify: componente del segmented de estados (localízalo: `grep -r "updateStatus" src/components/detail`) para abrir la hoja cuando `updateStatus` devuelva `{ kind: "askResume" }` y para abrir `close-pass-sheet` cuando devuelva `{ closed: true }`
- Modify: `messages/es.json`, `messages/en.json`

**Interfaces:**
- Consumes: `updateStatus(itemType, itemId, status, resume?)` (Task 5); `close-pass-sheet.tsx` existente.
- Produces: UX completa del cambio de estado; ningún componente llama a `updateStatus` sin manejar los dos resultados.

- [ ] **Step 1: `resume-pass-sheet.tsx`** — hoja (mismo patrón visual que `close-pass-sheet.tsx`: revisa sus imports y reutiliza el mismo contenedor) con dos botones:

```tsx
"use client";

import { useTransition } from "react";
import { useTranslations } from "next-intl";
import { updateStatus } from "@/lib/library/manage-actions";
import type { ItemType } from "@/lib/catalog/types";

// Al retomar un abandonado hay dos historias posibles: seguir el intento
// anterior (mismo pase, cursor y sesiones intactos) o empezar de cero (pase
// nuevo; el abandonado queda archivado en el diario). La máquina no decide
// sola: pregunta (Transition = askResume) y esta hoja responde.
export function ResumePassSheet({
  itemType,
  itemId,
  droppedAtLabel, // "dejado en pág. 210" — lo compone el padre desde position
  open,
  onClose,
}: {
  itemType: ItemType;
  itemId: string;
  droppedAtLabel: string | null;
  open: boolean;
  onClose: () => void;
}) {
  const t = useTranslations("passes.resume");
  const [pending, startTransition] = useTransition();

  const pick = (resume: "continue" | "restart") =>
    startTransition(async () => {
      await updateStatus(itemType, itemId, "in_progress", resume);
      onClose();
    });

  if (!open) return null;
  return (
    /* contenedor de hoja idéntico al de close-pass-sheet */
    <div role="dialog" aria-modal="true">
      <h2>{t("title")}</h2>
      {droppedAtLabel && <p>{droppedAtLabel}</p>}
      <button disabled={pending} onClick={() => pick("continue")}>{t("continue")}</button>
      <button disabled={pending} onClick={() => pick("restart")}>{t("restart")}</button>
      <button disabled={pending} onClick={onClose}>{t("cancel")}</button>
    </div>
  );
}
```

- [ ] **Step 2: Wiring en el segmented.** Donde hoy se llama `updateStatus(...)` y se ignora el resultado: guarda el `TransitionOutcome`; si `askResume` → estado local `showResume = true`; si `done && closed` → abrir `close-pass-sheet` con `outcome.passId` (el mecanismo de apertura ya existe para el flujo manual — reúsalo).

- [ ] **Step 3: i18n** — añade a `messages/es.json`:

```json
"passes": {
  "resume": {
    "title": "¿Retomar donde lo dejaste?",
    "continue": "Continuar donde lo dejé",
    "restart": "Empezar de cero",
    "cancel": "Cancelar"
  }
}
```

y su equivalente en `messages/en.json` ("Pick up where you left off?", "Continue where I left off", "Start over", "Cancel").

- [ ] **Step 4: Verificar en el navegador** (dev server) el ciclo: Leyendo → Dejado → Leyendo abre la hoja; "continuar" conserva el cursor; "de cero" lo pone a 0 y el diario enseña el abandonado.

- [ ] **Step 5: Commit**

```bash
git add src/components/detail/resume-pass-sheet.tsx messages/*.json src/components
git commit -m "feat(detail): hoja de retomar y encadenado de la hoja de cierre"
```

---

### Task 7: Sesiones sobre el pase y auto-cierre de libros

**Files:**
- Modify: `src/lib/sessions/actions.ts`
- Rename: `src/app/sesion/[entryId]/` → `src/app/sesion/[passId]/` (y sus imports/links: `grep -r "/sesion/" src`)

**Interfaces:**
- Consumes: `applyTransition`, `getActivePass` (Task 5), `planTransition`.
- Produces: `addSession(passId, itemType, itemId, prevState, formData)`; la sesión que alcanza el total redirige a `itemHref(...)?cerrar=<passId>` y la ficha abre la hoja de cierre al ver ese parámetro.

- [ ] **Step 1: Reescribir `addSession`.** Cambios sobre el fichero actual (mantén validaciones de duración, nota y edición tal cual):

1. La firma pierde `entryId` a favor de `passId`; la entrada ya no se consulta: `const pass = await getActivePass(...)` y si `!pass || pass.id !== passId` → `{ error: "generic" }`. Si el pase está en `planned`, primera escritura: `applyTransition(..., "in_progress")` (sustituye al bloque `openPass` actual de las líneas 83–93).
2. El insert de la sesión pierde `library_entry_id` y usa `pass_id: passId`.
3. El `<Select name="status">` del formulario deja de escribirse a mano: tras insertar la sesión, si el usuario eligió un estado distinto, `await applyTransition(supabase, user.id, itemType, itemId, status)` — NUNCA un `update` directo (este era el fallo 5 de la spec).
4. La posición se escribe en el PASE: el bloque que hacía `from("library_entries").update({ position, status })` pasa a

```ts
if (nextPosition) {
  const { error: updateError } = await supabase
    .from("diary_entries")
    .update({ position: nextPosition })
    .eq("id", passId)
    .eq("user_id", user.id);
  if (updateError) return { error: "generic" };
}
```

5. **Auto-cierre de libro** (después de escribir la posición):

```ts
// Regla 5 del esquema de flujo: si la sesión alcanza la última página de TU
// edición, el pase se completa solo — pasando por la máquina, no aparte — y
// la ficha encadena la hoja de cierre (parámetro ?cerrar).
const reachedEnd =
  itemType === "book" &&
  maxPosition !== null &&
  sessionPosition.page === maxPosition;
if (reachedEnd) {
  await applyTransition(supabase, user.id, itemType, itemId, "completed");
  revalidateItemViews(itemType, itemId);
  redirect(`${itemHref(itemType, itemId)}?cerrar=${passId}`);
}
```

6. En la página de ficha (donde vive `close-pass-sheet`), abre la hoja si llega `searchParams.cerrar` con un id de pase propio.

- [ ] **Step 2: Renombrar la ruta** — la página busca el pase activo (`getActivePass`) en lugar de la entrada; los enlaces hacia `/sesion/` pasan el id del pase activo.

- [ ] **Step 3: Verificar en navegador**: registrar sesión intermedia (posición avanza), sesión final (auto-cierre + hoja), y cambiar estado desde el propio formulario (pasa por la máquina: dejar un libro desde la sesión lo deja Abandonado con `finished_on`).

- [ ] **Step 4: Commit**

```bash
git add -A src/lib/sessions src/app/sesion src/app src/components
git commit -m "feat(sessions): la sesion cuelga del pase y cierra sola al llegar al final"
```

---

### Task 8: Series — cursor por pase

**Files:**
- Modify: `src/lib/series/episode-watch-store.ts`
- Modify: pestaña Episodios (localiza el componente: `grep -r "markEpisodeWatched\|episode_watches" src/components src/app`)
- Modify: `src/lib/sessions/actions.ts` (rama serie)

**Interfaces:**
- Consumes: `applyTransition`; `episode_watches.pass_id` (Task 2).
- Produces: `markEpisodeWatched(supabase, userId, seriesId, passId, season, episode)`; `rollSeriesProgress(supabase, userId, seriesId, passId)` que escribe `passes.position` con el episodio más avanzado DE ESE PASE y devuelve `{ reachedEnd: boolean }`.

- [ ] **Step 1: `episode-watch-store.ts`** — las escrituras de vistos añaden `pass_id: passId`; el upsert de conflicto (user, series, season, episode) pasa a incluir `pass_id` en el conflicto o inserta fila nueva por pase (decisión: **fila por pase** — el índice único viejo `(user_id, series_id, season_number, episode_number)` se sustituye en una mini-migración `20260716_pass_hub_b2_episode_unique.sql`:

```sql
-- Un episodio puede estar visto en varios pases (revisionado): la unicidad es
-- por pase. Los vistos legacy (pass_id null) conservan la suya con un índice
-- parcial para no duplicar historia antigua.
alter table public.episode_watches
  drop constraint if exists episode_watches_user_id_series_id_season_number_episode_num_key;
create unique index if not exists episode_watches_once_per_pass
  on public.episode_watches (pass_id, season_number, episode_number)
  where pass_id is not null;
create unique index if not exists episode_watches_legacy_unique
  on public.episode_watches (user_id, series_id, season_number, episode_number)
  where pass_id is null;
```

(aplícala en dev con MCP como las demás; el nombre real del constraint sale de `select conname from pg_constraint where conrelid = 'public.episode_watches'::regclass;`).

- [ ] **Step 2: `rollSeriesProgress` por pase** — el máximo se calcula con `.eq("pass_id", passId)` en vez de user+series; escribe `diary_entries.position` del pase; compara contra el último episodio de `series_episodes` de la serie y devuelve `{ reachedEnd }`.

- [ ] **Step 3: Auto-cierre de serie** — en la rama serie de `addSession` y en la pestaña Episodios: si `rollSeriesProgress(...).reachedEnd` → `applyTransition(..., "completed")` + redirección/estado con `?cerrar=<passId>` (idéntico al libro).

- [ ] **Step 4: Pestaña Episodios, dos capas** — la consulta trae los vistos del pase activo (capa cursor, estilo actual) y aparte los vistos de cualquier pase (`pass_id` distinto o null → chip atenuado, título i18n `passes.seenBefore`: "Visto en otro pase" / "Seen on an earlier watch").

- [ ] **Step 5: Verificar en navegador** un revisionado: nuevo pase de una serie terminada → cursor a cero, capa atenuada completa, marcar el último episodio auto-cierra.

- [ ] **Step 6: Commit**

```bash
git add -A src/lib/series src/lib/sessions supabase/migrations src/components src/app
git commit -m "feat(series): cursor y auto-cierre por pase; capa 'visto alguna vez'"
```

---

### Task 9: Re-apuntar todos los lectores de `library_entries`

**Files (todos Modify; lista cerrada — sale de `grep -rn 'from("library_entries")' src`):**
- `src/lib/library/get-library-items.ts`, `get-library-summary.ts`, `get-library-stats.ts`, `favorite-actions.ts`
- `src/lib/queue/get-queue-items.ts`, `get-movie-cadence.ts`, `get-reading-pace.ts`
- `src/lib/community/get-community.ts`
- `src/lib/social/feed.ts`, `notifications.ts`, `shared-activity.ts`, `recent-reviews.ts`
- `src/lib/stats/get-streaks.ts`, `get-annual-completed.ts`
- `src/lib/diary/get-monthly-activity.ts`
- `src/lib/challenges/get-challenge-progress.ts`
- `src/lib/clubs/activities/checkpoints.ts`, `src/components/clubs/club-post-actions.ts`
- `src/lib/import/commit-row.ts`, `src/app/api/export/route.ts`
- `src/app/libro/[id]/page.tsx`, `src/app/serie/[id]/page.tsx`, `src/app/pelicula/[id]/page.tsx`
- `src/lib/catalog/edit-actions.ts`

**Interfaces:**
- Consumes: columnas del hub en `diary_entries` (Task 1) y la vista `pass_reviews` ampliada.
- Produces: cero lecturas/escrituras de `library_entries` en `src/` (salvo `database.types.ts`).

- [ ] **Step 1: La transformación, fichero a fichero.** Patrón general — donde había

```ts
supabase.from("library_entries").select("...").eq("user_id", uid).eq("status", "...")
```

pasa a

```ts
supabase.from("diary_entries").select("...").eq("user_id", uid).eq("is_active", true).eq("status", "...")
```

y donde había el join embebido `library_entries!inner(item_type, item_id, status)` se leen las columnas directas del pase. Caso resuelto como referencia, `get-community.ts` (leído durante el diseño):

- consulta de notas: fuera el join — `from("diary_entries").select("id, rating, finished_on, user_id").eq("item_type", itemType).eq("item_id", itemId).not("finished_on", "is", null).not("rating", "is", null).neq("status", "dropped")` (el filtro de abandono pasa de la ENTRADA al PASE: es el cambio semántico validado en la Task 1, control de medias).
- consulta de reseñas: desaparece el paso previo por `library_entries` para sacar `entryIds`; `pass_reviews` ya expone `item_type/item_id`: `.eq("item_type", itemType).eq("item_id", itemId)` directamente.

En `favorite-actions.ts` (fijados): los updates de `pinned_order` van al pase activo (`.eq("is_active", true)`). En `get-queue-items.ts`: pases activos `status = 'planned'` con `queue_id`. En `commit-row.ts` (importador): crea pases (activo + histórico según el CSV) en vez de entrada+pase. En las páginas de ítem: "¿lo tengo?" = pase activo. En `feed.ts`/`notifications.ts`/`checkpoints.ts`/`club-post-actions.ts`: sustituye el join por columnas directas del pase, misma forma que get-community.

- [ ] **Step 2: Barrido de verificación** — `grep -rn 'from("library_entries")' src` debe devolver 0 resultados; `npx tsc --noEmit` limpio (ya no queda ninguna tarea pendiente de tipos).

- [ ] **Step 3: Vitest completo** — `fnm use 22.23.1 && npx vitest run`. Esperado: PASS.

- [ ] **Step 4: Commit**

```bash
git add -A src
git commit -m "refactor: toda lectura de biblioteca sale del pase activo"
```

---

### Task 10: Migración C — rename a `passes` y cordón de seguridad

**Files:**
- Create: `supabase/migrations/20260717_pass_hub_c_rename.sql`
- Modify: `src/lib/supabase/database.types.ts` (regenerar) y barrido `from("diary_entries")` → `from("passes")` en todo `src/`

**Interfaces:**
- Produces: tabla `public.passes`; `library_entries` congelada; el código solo conoce `passes` y `pass_reviews`.

- [ ] **Step 1: Escribir la migración**

```sql
-- La tabla del pase deja de llamarse como el diario: es el hub del registro
-- personal. El rename arrastra índices, FKs y políticas RLS (siguen colgando
-- de la tabla); la vista pass_reviews se recrea para que el repo diga la
-- verdad. Las etiquetas 'diary_entries' ALMACENADAS en comparticiones de club
-- e interacciones son datos, no nombres de tabla: NO se tocan.
alter table public.diary_entries rename to passes;
alter index if exists diary_entries_one_open_pass rename to passes_one_open;
alter index if exists diary_entries_one_pass_per_day rename to passes_one_close_per_day;

drop view if exists public.pass_reviews;
create view public.pass_reviews as
select
  d.id, d.user_id, d.item_type, d.item_id,
  d.status, d.is_active, d.position, d.pinned_order,
  d.started_on, d.finished_on, d.rating, d.review, d.is_public,
  d.edition_id, d.created_at
from public.passes d
where
  d.user_id = (select auth.uid())
  or (
    d.is_public
    and (
      public.can_view_profile(d.user_id)
      or public.is_visible_via_club_share('diary_entries', d.id, d.user_id)
    )
  );

grant select on public.pass_reviews to anon, authenticated;

-- El cordón: se corta el último lazo con library_entries y la vieja tabla
-- queda congelada como red de revert (se borra en una limpieza posterior).
alter table public.progress_sessions drop column if exists library_entry_id;
alter table public.passes drop column if exists library_entry_id;
revoke insert, update, delete on public.library_entries from authenticated;
```

Ojo: `pass_reviews` pierde `library_entry_id` — para entonces (Task 9 hecha) ya nada lo selecciona; verifica con `grep -rn "library_entry_id" src` ANTES de aplicar (0 resultados fuera de `database.types.ts`).

- [ ] **Step 2: Aplicar en dev** vía Management API y regenerar `database.types.ts` con el CLI (`npx supabase gen types typescript --project-id tyvzpuhxfwxrnkcpzxyg`).

- [ ] **Step 3: Barrido en código** — sustituir `from("diary_entries")` por `from("passes")` en todo `src/` (Task 9 dejó la lista exacta; `grep -rln 'from("diary_entries")' src` y reemplazo literal). `getInteractionSummary(supabase, "diary_entry", ...)` NO cambia: es la etiqueta de datos de las interacciones.

- [ ] **Step 4: Verificación completa** — `grep -rn '"diary_entries"' src` → solo apariciones como etiqueta de interacciones/comparticiones (documenta cuáles); `npx tsc --noEmit` limpio; `npx vitest run` PASS; `npm run build` OK. (Los advisors se revisan en prod tras el despliegue, Task 12 — el MCP no ve dev.)

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(db): diary_entries pasa a llamarse passes; library_entries congelada"
```

---

### Task 11: E2E de los flujos del esquema

**Files:**
- Create: `e2e/pase-hub.spec.ts` (confirma el directorio real con `npx playwright test --list` y `docs/TESTING.md` antes de crearlo; sigue el patrón de login/fixtures de los specs existentes)

**Interfaces:**
- Consumes: toda la funcionalidad de las tareas 5–10.

- [ ] **Step 1: Escribir los escenarios** (uno por regla del esquema de flujo; usa los helpers de autenticación y seed que usen los specs vecinos):

1. **Alta → Pendiente**: buscar un libro, añadir; la ficha muestra Pendiente y la biblioteca lo lista.
2. **Auto-cierre con hoja**: pasar a Leyendo, registrar sesión hasta la última página de la edición; la ficha vuelve con la hoja de cierre abierta; guardar nota; el diario muestra el pase Completado.
3. **Abandonar y retomar (ambas ramas)**: dejar a medias → Dejado; volver a Leyendo → hoja de retomar; "continuar" conserva la página; repetir con "de cero" y comprobar cursor 0 y dos pases en el diario.
4. **Película de un gesto**: marcar Vista → hoja de cierre directa, sin paso por Leyendo.
5. **Relectura**: sobre un Completado, Leyendo de nuevo → diario con 2 pases, el nuevo Leyendo con cursor 0.
6. **Serie con revisionado**: completar una serie corta marcando episodios (auto-cierre al último), abrir revisionado, comprobar capa atenuada y cursor propio.

- [ ] **Step 2: Ejecutar** — `npx playwright test e2e/pase-hub.spec.ts`. Esperado: PASS. Si el runner necesita dev server, usa el script que documente `docs/TESTING.md`.

- [ ] **Step 3: Commit**

```bash
git add e2e
git commit -m "test(e2e): ciclo de vida completo del pase (las 7 reglas del esquema)"
```

---

### Task 12: Verificación final y despliegue

- [ ] **Step 1: Suite completa en local** — `fnm use 22.23.1 && npx vitest run && npm run build && npx playwright test`. Todo PASS.

- [ ] **Step 2: Push y PR** — la rama `worktree-pase-hub-spec` ya tiene PR draft (#42, la spec): push de todos los commits y actualizar la descripción del PR con el resumen de migraciones (A, B, B2, C) y el aviso de que **prod requiere aplicarlas en orden tras el merge**.

- [ ] **Step 3: (Post-merge, con el usuario) Aplicar en PROD** las cuatro migraciones en orden con las mismas consultas de control de las tareas 1 y 2 antes/después, y `get_advisors` al final. Lección del 2026-07-14 en la memoria del proyecto: mergear NO aplica migraciones; comprobar prod contra el repo antes de dar por desplegado.
