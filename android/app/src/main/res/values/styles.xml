<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      El splash de arranque. Dos caminos, y el sistema elige por version:

      - Android 12+ (API 31+) dibuja SU propio splash y no mira
        `android:background`: usa `windowSplashScreenBackground` con el icono
        dentro de un circulo. Sin estos dos atributos salia el terracota con el
        icono por defecto.
      - Android 11 y anteriores caen en `android:background`, que es
        `@drawable/splash` (terracota + la marca, ya en vectores).

      El terracota es el mismo `background_color` del manifest de la PWA, para
      que el arranque nativo y el web se vean iguales. Ver
      docs/requirements/decisiones.md.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_mark</item>
    </style>
</resources>