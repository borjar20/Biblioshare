<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.google.mlkit.vision.DEPENDENCIES" android:value="barcode_ui"/>

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Widgets nativos (Jetpack Glance). exported=false: solo el sistema
             les manda APPWIDGET_UPDATE. -->
        <receiver
            android:name=".widgets.CurrentProgressWidgetReceiver"
            android:label="@string/widget_current_progress_label"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/current_progress_widget_info" />
        </receiver>
        <receiver
            android:name=".widgets.DailyGoalWidgetReceiver"
            android:label="@string/widget_daily_goal_label"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/daily_goal_widget_info" />
        </receiver>
        <receiver
            android:name=".widgets.QuickRegisterWidgetReceiver"
            android:label="@string/widget_quick_register_label"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_register_widget_info" />
        </receiver>

        <!-- Foreground service: espeja el cronómetro nativo (TimerStore) en una
             notificación persistente mientras hay una sesión de lectura activa. -->
        <service
            android:name=".reading.ReadingSessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Cronómetro de la sesión de lectura activa, visible para el usuario" />
        </service>

        <!-- Botones de la notificación de sesión (pausa/reanuda/terminar/descartar). -->
        <receiver android:name=".reading.ReadingSessionReceiver" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Push (spec item 2): permiso en tiempo de ejecución de Android 13+ (API 33).
         En 12 y anteriores las notificaciones se reciben siempre; este permiso
         solo gobierna el prompt de POST_NOTIFICATIONS que dispara requestPermissions(). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Foreground service de la sesión de lectura (notificación persistente). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
</manifest>
