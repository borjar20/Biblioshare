// Placeholder de Task 4: round-block.tsx importa RoundHistory y la maqueta
// pinta la tira "Rondas anteriores" debajo del bloque, pero la lectura del
// histórico (listRoundHistory) es la Task 5 del plan -- no el alcance de
// esta tarea. Se deja el nombre de fichero/componente y la forma de props
// exactos que pide Task 5 para que la sustituya sin tocar round-block.tsx.
export function RoundHistory({
  clubId,
  currentPeriodKey,
}: {
  clubId: string;
  currentPeriodKey: string;
}) {
  void clubId;
  void currentPeriodKey;
  return null;
}
