import type { ActivityKindDefinition } from "./types";

// Stub -- Bloque H2 construye el comportamiento real (club_activity_placements).
export const tierlistKind: ActivityKindDefinition = {
  kind: "tierlist",
  allowedItemTypes: "all",
  maxItems: null,
};
