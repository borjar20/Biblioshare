import type { ActivityKindDefinition } from "./types";

// Stub -- Bloque H3 construye el comportamiento real (progreso derivado de
// library_entries/diary_entries, sin tablas nuevas).
export const listChallengeKind: ActivityKindDefinition = {
  kind: "list_challenge",
  allowedItemTypes: "all",
  maxItems: null,
};
