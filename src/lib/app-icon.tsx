export function AppIconMark({ size }: { size: number }) {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: "#2c002b",
        color: "#ffe9fc",
        fontFamily: "serif",
        fontSize: size * 0.58,
        fontWeight: 600,
      }}
    >
      B
    </div>
  );
}
